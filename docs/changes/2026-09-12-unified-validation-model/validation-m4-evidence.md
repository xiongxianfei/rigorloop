# Validation M4 execution evidence

Owning change: [unified Validation](change.json). Engineering base: `5c9de93b`. [Observed case identities](validation-m4-case-populations.json) record the actual complete population and equality of discovered, started and completed sets across normal and individual runs. This file is historical execution evidence, never an active discovery inventory or saved validation result.

The catalog explicitly adopts eight selected/broad/main identities for three Python suites: selector, artifact lifecycle and change metadata. Literal command/adapter bases identify the new `python-unittest` units. Remaining suites retain command units and conservative constraints; this is no fourth-suite or inventory-wide case-isolation claim. Contradictory case-unit command shapes reject even when their supplied command basis matches.

The internal adapter executes each original script as `__main__` with its original arguments and intercepts only its outer unittest runner. Discovery therefore uses the normal loader, `load_tests`, name/pattern handling and dynamic fixture preparation. It performs no test methods and supplies no passing validation evidence. Workers invoke the same script with its exact `Class.test_method` selector in a fresh process. The original runner handles setup, teardown, subtests, failure output and normal result semantics; nested test runners are not intercepted. Existing direct commands and selective reproduction remain available. No new public runner or skill is introduced.

Each worker writes an invocation-owned completion receipt. The executor checks the exact required identity, one actual run, complete start/stop records, successful outcome and absence of skips. Missing, malformed, unknown-field/value, mismatched or stale receipts cannot turn exit zero into a pass. A fresh launch removes any previous receipt; failed collection, zero cases, duplicate identities, loader errors and worker disagreement fail visibly. Required skips fail. Results retain actual bootstrap argv and a separate usable original-script rerun command. Case groups expand before execution and every dependency/diagnostic ordering edge binds to all required cases; one failed case prevents its consumer from launching.

The isolation audit covers all 278 surviving cases. Selector fixtures create owned temporary Git/workspace trees, register cleanup and read immutable repository inputs; loader name patterns restore process-local state. Synthetic CI tests explicitly declare their controlled scripts as command units in their fixture catalog, while actual unit adoption is separately exercised through the real repository wrapper. Lifecycle fixtures copy their input trees and use independent current-record/Git roots; module/global behavior remains process-local. Metadata fixtures own temporary roots and copy current v3 JSON inputs. No selected suite has a shared mutable module/class fixture that requires ordered prior tests. The original scenario subtests remain within their owning case. Existing cache-only retirements occurred in M1; M4 removes no case. The selector catalog assertion is renamed to describe audited commands and the current initial case population, retaining its exact-set protection.

The three scripts' environment changes and nested validator/CI calls remain inside their own processes/temporary roots. Nested schedulers receive only the case allocation; native Node tests retain explicit allocated concurrency. Case expansion preserves a declared serial flag and demand instead of overriding them: a red exclusive-marker regression exposed that implementation defect and passes after correction. The selected real cases use one worker each, with observed cross-case overlap when two are available. They require no external service or shared installation destination. Process-tree cleanup, timeout, signal, output and failure-order contracts remain in the shared executor.

Observed proof:

- `python scripts/test-select-validation.py`: 163 passed; `python scripts/test-artifact-lifecycle-validator.py`: 108 passed; `python scripts/test-change-metadata-validator.py`: 7 passed.
- `python scripts/test-validation-execution.py`: complete standalone 36-test basis passed, including each selected suite through its original full entrypoint, every individual case with jobs1, and reversed individual order with jobs2. All 163/108/7 discovered, started and completed identity sets agree. Jobs1 shows no overlap; jobs2 shows actual overlap for every suite.
- Two subsequent focused red/green checks, `CaseAdapterTests.test_case_expansion_preserves_declared_serial_resource_constraint` and `CatalogTests.test_case_unit_rejects_contradictory_command_before_launch`, preserve explicit serial demand and reject contradictory case units. Both pass. The resulting complete current 38-test executor entrypoint also runs as a required leaf of both final broad commands below, without filtering or test stubs.
- The actual selected-wrapper metadata expansion test passes all seven case leaves. Independent review separately ran real collection, receipt, custom/dynamic discovery, dependency and resource-constraint negatives.
- `bash scripts/ci.sh --mode broad-smoke --jobs 1` and `bash scripts/ci.sh --mode broad-smoke --jobs 2`: both passed all 286 required tasks on the same current engineering basis. Three former command leaves expand into 278 actual case leaves; eight other required commands remain. No changed current-record root is selected for this script-only dirty scope. Each command includes the full current executor integration suite, whose nested tests obey the outer allocation.
- `git diff --check`: passed.

The final integration commands run in an isolated Linux filesystem checkout of the authored HEAD plus working diff, preserving the same tracked inputs and declared dirty scope. M3's Windows-mounted filesystem timeout limit remains explicit; no timeout was raised or case omitted. Elapsed time is observed only, not a promised performance improvement. Actual case receipts and captures are scoped temporary files and are discarded after execution; no validation-result cache is created.

M5 source/consumer retirement and product integration, fresh whole-change Code Review and distinct final Verify remain outstanding. M4 does not claim final readiness or customer-policy activation.
