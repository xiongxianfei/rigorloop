# Validation M3 execution evidence

Owning change: [unified Validation](change.json). Engineering base: `2326baf2`; initial composition candidate `075905f8`, plus the exact correction subjects registered with this evidence.

Broad-smoke and main now resolve their membership and commands from the current catalog and execute through the same bounded module as selected CI. The wrapper retains argument parsing, contributor modes and Release-owned `check-ci` interception. Broad-smoke has twelve conditional leaves (eleven without changed current record roots); main preserves its twenty-seven direct product/governance leaves and original version-specific archive commands. Added selector/executor regression leaves supply current graph and execution proof. Package build and validation share one invocation-owned output; validation depends on successful build. Generated adapter/runtime paths remain excluded from dirty authored lifecycle scope. Changed current record roots use the current metadata validator as a preflight dependency.

Selected broad-smoke groups expand inside the same graph and budget. Normal focused failures prevent dependent boundary execution; explicit diagnostic broad-smoke waits for focused completion and retains the original failure. Blocked selection still returns its original status after explicitly requested diagnostics. All independently required work completes by default. Unknown composed membership and retired classification override reject. Result JSON retains its current-run fields, stable child labels and `cache_status: not-applicable`; historical baseline fields are null with an explicit unavailable-comparison limitation.

The old broad Bash schedulers, embedded selected scheduler and historical classification checker are removed. Eight selector assertions/helpers exclusive to historical classification schema or Bash label scraping were retired after catalog/graph protection passed. Three real wrapper composition tests replace the execution-boundary portion: failed build prevents archive validation while independent proof continues; selected broad-smoke uses one wrapper invocation; blocked/failed diagnostic runs preserve the original failure. The June 26/27 classification and June 27 baseline bytes remain unchanged and have no active runtime reader. The historical retirement inventory projection excludes current-only mode IDs instead of rewriting its original ledger.

Real integration exposed two stale shell-source consumers. The adapter suite now observes literal generated-package commands, the shared output dependency and generated-path exclusion in a real isolated Git composition fixture. The review-artifact suite observes the current metadata command, exact root and preflight dependency in its own Git fixture. No old-path marker was added to production merely to satisfy an assertion. Release candidate proof also exposed five stale current model navigation links in the Skill archive README; their targets and labels were rebased. Archived source bytes and their hash assertions remain unchanged.

The first actual one-worker broad run exposed ten selector test assumptions about nested concurrency. The cases now retain the same failure/ordering assertions while respecting the parent's allocation: one worker proves sequential behavior and unstarted fail-fast peers; two available workers still require observed overlap and completion of started peers. No case is skipped. A red real-child regression also exposed that direct calls to the shared module could exceed an inherited allocation; the module now caps its own jobs and rejects invalid parent values before launch. Both wrapper and internal entrypoints therefore bound nested worker pools. Python bytecode writes are disabled for CI descendants so execution does not create untracked authoritative script inputs in isolated fixtures.

Observed commands and results:

- `python scripts/test-select-validation.py`: full composition basis 163 passed. After nested-budget corrections, `RIGORLOOP_VALIDATION_WORKERS=1 python scripts/test-select-validation.py`: all 163 passed. The final broad two-worker run executes the complete corrected selector suite with two workers.
- `python scripts/test-validation-execution.py`: final 26 passed; `RIGORLOOP_VALIDATION_WORKERS=1 python scripts/test-validation-execution.py`: 26 passed. New proof includes closed composed-mode membership, real package dependency/diagnostic boundaries and inherited-budget unknown-value/actual-child checks.
- `python scripts/test-adapter-distribution.py AdapterDistributionTests.test_ci_script_runs_adapter_checks_and_filters_generated_paths`: passed after the old assertion was reproduced failing.
- `python scripts/test-review-artifact-validator.py ReviewArtifactValidatorFixtureTests.test_ci_script_keeps_review_regressions_and_validates_current_record_roots`: passed after the old assertion was reproduced failing.
- `python scripts/test-skill-validator.py SkillOwnerTransferTests.test_skill_owner_current_contract_and_archive_identity`: passed; archived originals remain hash-bound.
- `python scripts/test-release-transaction.py`: final 164 passed, including actual candidate generation, packed metadata and no-publication fixtures. Earlier runs exposed the archive navigation and adapter consumer failures; one earlier concurrent edit invalidated a fixture fingerprint and was rerun on frozen source. No live release was executed.
- `python scripts/test-retirement-ledger.py`: 17 passed for the current-only catalog projection; later corrections do not change that inventory basis.
- `bash scripts/ci.sh --mode broad-smoke --jobs 1` and `bash scripts/ci.sh --mode broad-smoke --jobs 2`: each passed all twelve required leaves on one identical engineering basis. Neither uses a stub, classification override, diagnostic bypass or skip-diff option.
- `git diff --check`: passed.

Environment limit: the first broad run in the Windows-mounted workspace hit the unchanged 300-second adapter-suite timeout as well as the supported consumer/budget failures. The final complete sequential and parallel broad checks ran in an isolated Linux filesystem checkout with the same HEAD and applied working diff. A byte comparison of every tracked source found no difference from the authored workspace; generated package output and temporary case roots remain invocation-owned. This records the filesystem-specific limit rather than raising the timeout or omitting the full suite. No speed improvement or Windows-filesystem broad success is claimed.

M3 retains opaque command suites except the earlier bounded command assessments. Actual independent case discovery/execution remains M4; source and published-consumer consolidation remains M5. These results do not establish final Verify or PR readiness.
