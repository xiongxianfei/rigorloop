# Refine living Designs into current contracts

## Authorized scope and execution outline

The user explicitly authorized removing obsolete stage queues and completed migration inventories across all twelve Designs, after the earlier presentation-only correction. This is scoped Design authoring; earlier advisory and authoring evidence retain their original subjects and limitations. No new model, public command, stored format, review gate or runtime behavior is selected.

1. Capture the current working-tree subjects, including the prior uncommitted presentation edits. Verify that removed historical content remains recoverable in Git.
2. Remove Next artifacts, Follow-on artifacts and equivalent original planning handoffs. Keep Workflow's current coordination and follow-up placement contracts.
3. Replace completed source-transfer/file-removal inventories with concise current-owner and compatibility guidance. Preserve requirements, decisions, examples, diagrams and technical/proof constraints; retain mixed operational material rather than delete it by heading.
4. Resolve current links to removed sections. Keep accepted deferred work in its existing follow-up register without changing its status. Preserve old assessments and plans unchanged.
5. Remove the obsolete Next artifacts prompt from the canonical living-Design skeleton so new models do not recreate it. Retained legacy architecture scaffolds remain under their separate format.
6. Check source preservation, current-owner coverage, Markdown links and repository-selected validation, then record exact authored subjects for independent Design Review. No independent approval or final Verify is claimed by the author.

## Governing basis and preservation

Constitution's current-owner, stage-ownership and historical-retention rules govern. System retains the existing hierarchy. Design owns the model document convention; Workflow owns current progression and follow-up placement; Assessment owns review and applicability. Technical acceptance, stored-format rejection, installer safety, package integrity and release authorization remain with their existing owners.

The removal baseline is commit `38a3042e63c7c2462ecf8ffed29f4ac0cbb8923f` and the exact paths below. The prior presentation edits are separately captured before refinement and are preserved where still useful. Historical mappings are provenance, not a prerequisite for current operation or evidence that an old review approves a new subject. New current constraints do not depend on fetching deleted text from Git.

| Selected material | Current destination or disposition |
| --- | --- |
| All model Next/Follow-on sections; Validation's original planning/current follow-through handoff | Remove stage queues; current coordination remains in Workflow and actual work remains in its owning records/plans. |
| System's original mixed-architecture and necessary-design inventories | Keep responsibility inventory, shared interfaces, integrated acceptance and current retirement boundaries; retain original mappings in baseline `docs/design/system.md`. |
| Design's requirement/example transfer inventory and original consumer rollout | Keep model/example contract, retention policy and embedded decision rationale; baseline `docs/design/skill/design.md` retains old source IDs and mapping details. |
| Skill's completed common-contract/readability/portability/resource transfer inventories | Keep common contract details, specialist behavior, resources and decisions; FU-015–018 retain accepted future adoption work. Baseline `docs/design/skill/skill.md` retains transfer history. |
| Workflow's original adoption/consumer and WF-MAP inventories | Keep current v3 coordination, authoring ownership, failure/recovery and retirement compatibility; baseline `docs/design/skill/workflow.md` retains WF-MAP identities and their historical scope. |
| Assessment's extraction/consumer maps | Keep shared assessment rules and specialist scope/attribution; baseline `docs/design/skill/assessment.md` retains source-clause extraction history. |
| Validation's source/test-spec/cache-script removal inventories | Keep current proof criteria, executor behavior, retired-input rejection and privacy/recovery constraints; baseline `docs/design/engineering/validation.md` retains original deletion targets and source-qualified test mappings. |
| Packaging's Distribution spec/ADR and script removal inventories | Keep artifact representation, installer boundary, real-boundary proof and current supported/retired surfaces; baseline `docs/design/engineering/packaging.md` retains original TNI/MAI/lockfile mappings. |
| Release's source/ADR/consumer migration inventories | Keep current qualification, representation, candidate/evidence interfaces, publication/recovery and the live literal baseline; baseline `docs/design/engineering/release.md` retains original REL/Transaction mappings. |
| CLI, Records, Installation and Engineering drafting/handoff prose | Keep substantive current contracts, including operational retirement tables and supported recovery; baseline paths retain original handoffs. |
| Canonical living-Design skeleton | Remove the stage-handoff prompt; preserve engineering sections. Legacy-format scaffolds are outside this correction. |

## Validation and recovery allocation

Before/after comparisons must preserve each model's normative requirement table, stable decision rows, Mermaid bodies and example files. Inspect removed prose for technical obligations that need explicit retention. Check current local links and reconcile affected follow-up source links without settling their work. Retain historical record bytes and original subjects; report old-evidence applicability limits instead of editing history.

Run repository-selected CI for the twelve model paths, the changed living-Design skeleton and any current navigation consumer, plus focused model checks and diff hygiene. Source/resource changes require the selected skill/package checks; a documentation extension is not an exemption. Do not add a permanent test that merely requires these deleted headings to remain absent. If a current validator or consumer has a material dependency, preserve or reconcile it before removing its source.

Restore only a failed refinement slice from its captured working-tree basis after checking for intervening edits. No whole-tree reset, deletion of operational sources, archive duplication, external action or historical-record rewrite is authorized by this refinement.

## Authored result and preservation checks

Removed obsolete Next artifacts, Follow-on artifacts and drafting-stage handoffs from all twelve living Designs. Completed source inventories now resolve through exact commit/path provenance. Current compatibility, consumer interfaces, specialist assessment methods and proof protections remain in their owning models. The normal Design skeleton no longer prompts a stage queue; legacy architecture scaffolds retain their separate format. Current follow-up links were reconciled without changing accepted scope, status or ownership.

Compared against the captured working-tree basis: all twelve Requirements sections are byte-identical, including 257 normative requirement rows; 61 stable decision rows and 62 Mermaid bodies are unchanged. Design's material-decision preservation table remains in the current document. No example file or historical record was edited. A first broad row-matching check also counted historical transfer-map rows and therefore reported a mismatch; the corrected check compares the actual Requirements sections and passed.

The focused `python scripts/validate-boundary-first.py --check` invocation selected each of the twelve model paths with `--path` and passed (structure and references only). A temporary Markdown audit resolved all 500 local links across the models, follow-up register and skeleton. It initially found six references to removed Workflow anchors; these were corrected and the final audit passed. No live consumers of the removed anchors were found in canonical skills, templates, scripts, schemas, current navigation or governance. `git diff --check` passed. These checks support preservation and structural correctness, not independent semantic approval.

## Exact authoring subjects

The before identities include the earlier presentation edits. The after identities cover this refinement; old assessments retain their original subjects and are not presented as approval of these bytes.

| Path | Before identity | Authored identity |
| --- | --- | --- |
| `docs/design/cli/cli.md` | `sha256:eef25314c2f34d4cceca2b8e2a12675fd9773da34fa949fd92cb224ed6a456e7` | `sha256:12d601e7db59aa57380ad4a2d635ec1ba05ba6ac2477cd822688898a6096e504` |
| `docs/design/cli/installation.md` | `sha256:756a282dbfe5f363eb4eb39986e0f68cf607501bfb9de2a98d96de66b0819578` | `sha256:5320ca604afb4e0a224317e80e9907c2fc42b78f5ab6322bc81b45d8687d6574` |
| `docs/design/cli/records.md` | `sha256:c088b648eb0a8308c8cae1deb7f5be562cf5fb81ceed2552d5256851ca69acf7` | `sha256:8bcdb2872f9b6887212264493f1421a58b91b9b7ab9d12bcd240bcd5a9b5f966` |
| `docs/design/engineering/engineering.md` | `sha256:42fab5881ecf333e0e88ecbb6465ee47d75bdd8f54d9c465797bc13e85b19664` | `sha256:a29cdbaadcdbaf038a01b476bf290e6d1702da2e89faba230b82be120e6a10f8` |
| `docs/design/engineering/packaging.md` | `sha256:6dfe2436431bc52be7b3715e9249559bb7a893b08f6211e0a43b1a936b6096fd` | `sha256:0c9f56b406cf083682f7c353387dbcc2eff59cf4039aaafc7c6e01aa657de8d2` |
| `docs/design/engineering/release.md` | `sha256:4e64ece2c8b1147778fa31becc2ecb1d08469b7610cf53d29ecf40b49865441e` | `sha256:f07bd1f3bb74889d4eb6dcba223c9dc1374d7843775cfc027c72de6b0bff511a` |
| `docs/design/engineering/validation.md` | `sha256:a8e0b1f979c2eb004e0a370a944895df81b29d093543b1a3913d26efb6c61607` | `sha256:52b528ec2c16f6443ab26cb10adf291de7741c0dbddf188599de8fa9a34c4dd5` |
| `docs/design/skill/assessment.md` | `sha256:ae3b59ec189fd66962ddb3f50a4c0e48422fc9a1086aae641c54dd1481fd715b` | `sha256:7fb1a51ba101736b53ced11f6b4cfc9a12cddf310ede2135f0e81fa8da9a7ff9` |
| `docs/design/skill/design.md` | `sha256:898080330861918288787f6d5c713516429a32818ecc3413892f5d4ab3c61ab8` | `sha256:25ee82c2b8ee9e0405ce7ec6ba8a7061335160c69e4405c3e1464160eaa9ee56` |
| `docs/design/skill/skill.md` | `sha256:f599ec7d5f0c330a2c09e6148e4ee25f5af724b114d957e8c196b7550104da36` | `sha256:fc1d4c90c47d87e9e1eedca26749a99cb17c11d62d78b6332ff9f7c6ec1e76e9` |
| `docs/design/skill/workflow.md` | `sha256:ca2ba146c7cd45409b8b37a22d2baece26efed668f3ddad9309f495056ee1623` | `sha256:82d76b6dab99fa92731e6c6dbf083065abe303d5ee31576d15468f5ee7e996a7` |
| `docs/design/system.md` | `sha256:3bc11325ae7ef3474964bd08204066500f564b492c7794be6fcd478dc209df76` | `sha256:ca85e9445c2cd551caf0785173dfa7b235365bbee19293ec94fd6cc65cbaac09` |
| `skills/design/assets/design-skeleton.md` | `sha256:a83862ec20aaa6f2c7cfab96dfd427e88ed73960d1d777e7e9eab5a39d33ad1d` | `sha256:004189d8bbf456a020165b74cdf1f322dc1d2b3ce0b14e81079c76963fdffed6` |
| `docs/follow-ups.md` | `sha256:944a7c3200764ac7da6607011ccdd1b515b654a976bfbc4a6a0036b5fa430bd2` | `sha256:6705f032e42c06511b761ae883e13abe7ee057fbd081bc387120d55ba0266d36` |

## Repository validation and correction

Ran `bash scripts/ci.sh --mode explicit` with `--path` for each of the fourteen subjects above. The selector ran record-store schema, model validation, boundary-first regression, change-metadata regression, CLI tests, skill validation/regression and adapter regression. It reported 783 passing rows and one exited test: `OptionalDiscoverySkillContractTests.test_discovery_shared_policy_is_admitted_by_skill_contract`. The deleted Skill transfer table was the only declaration naming the adopted `discovery-support` shared-policy family. Restored that still-current admission next to common contract details; no test was weakened.

The exact failed test then passed directly. Reran the complete `python scripts/test-skill-validator.py` suite after correction: all 280 tests passed. Rechecked the changed Skill model with `python scripts/validate-boundary-first.py --check --path docs/design/skill/skill.md`: passed. The final 500-link audit and `git diff --check` passed. The initial CI invocation remains a recorded failed invocation; the targeted correction and complete affected-suite rerun establish its resolution without claiming a second full CI run. No hosted CI or publication was performed.

## Independent review handoff

Review the fourteen exact authored subjects above and this preservation/disposition account. Relevant unchanged dependencies include Constitution, Workflow/Assessment gate semantics, current model validators and schemas, Design's legacy scaffolds and packaged methods, all existing model-owned examples, and the accepted follow-up entries. The removed inventories are recoverable at the stated baseline paths. Earlier advisory/presentation evidence is historical and does not approve this new package.

Authorship and mechanical checks are complete. Independent Design Review and any subsequent governed approval remain separate; no existing historical change's completion or judgment has been modified or borrowed.

## Authoring decomposition: authorized execution outline

The user subsequently selected `docs/design/skill/authoring/{authoring,proposal,design,plan}.md` and authorized refinement. Preserve the earlier subject identities and validation above as evidence of their original bytes. This subsequent isolated authoring correction extracts Proposal and Plan contracts, gives Authoring explicit composition ownership, and moves Design without changing its stable IDs or public skill behavior.

Sequence: capture the current working-tree basis; extract specialist prose and preserve shared Skill requirements; author composition, requirements, decisions and acceptance scenarios for the new owners; reconcile System/Skill navigation, live links and exact validator path declarations; update existing path-recognition and deleted-path routing tests; run focused checks and selected repository CI; append final subjects, preservation and actual outcomes here. Historical plans and records retain their original paths and judgments. A local snapshot preserves uncommitted source content before moving it. No new authoring-record file or public skill is required.

Concrete compatibility work is restricted to `scripts/model_layout.py` and its existing model/selector tests. Exact declared paths remain the admission boundary; retired-path aliases apply only when absent, never to conceal an existing invalid file or symlink. Existing customer defaults and published skill methods remain unchanged. Review authority remains with independent Assessment; no historical approval is reassigned.

## Authoring decomposition result and preservation

The declared layout is now `docs/design/skill/authoring/authoring.md` with sibling `proposal.md`, `design.md` and `plan.md`. System owns the project path declaration; Skill delegates Authoring composition, and Authoring delegates each specialist. There are fifteen current model documents. No published invocation, record format or review gate changes.

| Original responsibility | Current owner and preservation |
| --- | --- |
| Skill → Authoring diagram, capability boundaries and refinement relationship | Authoring AUTH-SR-01–05, responsibility inventory and composition/runtime/context views. Workflow and Assessment remain external owners. |
| Skill → Proposal content and Proposal procedure | Proposal PROP-SR-01–06 and the complete extracted sections, with original SKL requirement applicability and corrected relative links. |
| Skill → Delivery allocation and Plan assets | Plan PLAN-SR-01–07 and the complete extracted sections; omit only the completed legacy-spec handoff paragraph. |
| Skill SKL-DEC-06 | Move its exact decision row to Plan without renumbering or changing its initialization limits. |
| `docs/design/skill/design.md` | Move to `docs/design/skill/authoring/design.md`; retain DES IDs, requirements, decision rows, diagrams and fenced example. Rebase relative links and identify Authoring composition and Plan’s delivery ownership. |
| Current live references and model-path consumers | Reconcile governance/navigation and affected Designs; update exact `PROJECT_MODEL_PATHS`, absent-old-path routing and existing path-recognition tests. Historical record/proposal/plan links retain their original revision meaning. |

Preservation comparison against the captured pre-decomposition working tree passed for the complete Design Requirements section, all DES decision rows and Mermaid bodies, the Skill Requirements section, all extracted Proposal/Plan/asset prose after heading/link normalization, and the exact moved SKL-DEC-06 row. The three new owners define requirement-linked acceptance scenarios and reasoned supporting-view decisions. Existing model-owned examples remain unchanged; the inline Design example moves with its owner.

`python scripts/validate-boundary-first.py --check` passed for all fifteen models and their selected examples. `python scripts/test-boundary-first-validation.py` passed all 61 tests after the final path-test correction. The final current-link audit resolved 583 local Markdown links across all models, follow-up navigation, the Design skeleton, Constitution and README. A stale temporary audit still selected the deleted path and failed; replacing its selection with the current catalog resolved that tooling-only error.

Initial local CI preflight rejected untracked new authoritative documents. Added intent-to-add entries for the four new paths, leaving their contents unstaged, then reran local CI. This was a repository tracking prerequisite, not user approval or a commit.

### Authoring decomposition subject set

These identities supersede reliance on the earlier authored package for this refinement only. Earlier evidence remains unchanged above; none of its judgments is retargeted. The set includes the current fifteen-model composition and the five changed governance/navigation/path-consumer subjects.

| Subject | Identity |
| --- | --- |
| `docs/design/system.md` | `sha256:f89029ffd80ff62482fa6d2956e01f34b0348430771198eae041e4664d90b6e2` |
| `docs/design/skill/authoring/authoring.md` | `sha256:4b75b6738fb94bac2272395dfeffce9eaa760e0fdb8ac241c9e90027cd3e7071` |
| `docs/design/skill/authoring/proposal.md` | `sha256:c476948880f22deaf4f332f6a0e2af1e6eba378869931b86bba501866a0b8bdf` |
| `docs/design/skill/authoring/design.md` | `sha256:0f7e41a1de83d542d9291a842876ae7c75ebb214242cca18ee825f539f52b778` |
| `docs/design/skill/authoring/plan.md` | `sha256:5ea24213d164d301ed4690f3647bbb98d8f5709bf10af03c3efbdcad08ef9826` |
| `docs/design/skill/workflow.md` | `sha256:7ce79b9b46f7a9c201a7750146b7cc536e2c982645a07d041e9a88c1de4c779e` |
| `docs/design/skill/assessment.md` | `sha256:af968c5583c27abb95e9a2b2ac0293deba30c522e72480c6f9aa05066659ae19` |
| `docs/design/cli/records.md` | `sha256:8bcdb2872f9b6887212264493f1421a58b91b9b7ab9d12bcd240bcd5a9b5f966` |
| `docs/design/cli/installation.md` | `sha256:5320ca604afb4e0a224317e80e9907c2fc42b78f5ab6322bc81b45d8687d6574` |
| `docs/design/engineering/validation.md` | `sha256:52b528ec2c16f6443ab26cb10adf291de7741c0dbddf188599de8fa9a34c4dd5` |
| `docs/design/engineering/packaging.md` | `sha256:0c9f56b406cf083682f7c353387dbcc2eff59cf4039aaafc7c6e01aa657de8d2` |
| `docs/design/engineering/release.md` | `sha256:f07bd1f3bb74889d4eb6dcba223c9dc1374d7843775cfc027c72de6b0bff511a` |
| `docs/design/skill/skill.md` | `sha256:dd2c54a74f438d4cdc0268fb2ca77fa863b54de9ddc336c882adefcae8915ab9` |
| `docs/design/cli/cli.md` | `sha256:12d601e7db59aa57380ad4a2d635ec1ba05ba6ac2477cd822688898a6096e504` |
| `docs/design/engineering/engineering.md` | `sha256:a29cdbaadcdbaf038a01b476bf290e6d1702da2e89faba230b82be120e6a10f8` |
| `CONSTITUTION.md` | `sha256:cc1912d5c0cd17b42bfd4a3f705a561b0c2f678045f0659f2406a1ea13e6c5a5` |
| `README.md` | `sha256:f5209814d0db97fca44892b0bbf45f8d2f58752d8661b8fe4a9997e70383a17e` |
| `scripts/model_layout.py` | `sha256:f606860bb621411a2e65fa94013b424b70e1831af3a8eb28801e50664950da27` |
| `scripts/test-boundary-first-validation.py` | `sha256:77fa8849e456106f0999f29546f7851e96bb483dd830162fcd498fc637628c4a` |
| `scripts/test-select-validation.py` | `sha256:1888aed139636c7aa75c039cb40aba0177571da3b37baa9738f2f411e7abe519` |

### Final Authoring validation and handoff

`bash scripts/ci.sh --mode local` passed after the tracking prerequisite was resolved: 1020 passing check/case rows across `adapters.regression`, `boundary_first.regression`, `boundary_first.validate`, `change_metadata.regression`, `current_records.validate`, `documentation_prose.enforce`, `guide_system.validate`, `markdown_readability.validate`, `model.validate`, `readme.validate`, `readme.vision_markers`, `record_store.schema`, `rigorloop_cli.test`, `selector.regression`, `skills.regression`, `skills.validate`, `validation_execution.regression`. The run includes the actual model catalog and deleted-path selector consumers, skill and adapter checks, CLI/record checks and validation-execution regressions selected from the complete working tree. This validates the combined earlier refinement and current Authoring decomposition; it is not hosted CI or independent semantic approval.

`python scripts/validate-documentation-prose.py --mode enforce --path README.md --path VISION.md` passed with zero errors/warnings. All 57 existing model-owned example files were byte-identical to the pre-decomposition snapshot. Final subject digests above were rechecked after CI; `git diff --check` passed.

The authored package is ready for independent Design Review of Authoring composition, its three specialist owners, affected parent/consumer references and the bounded path-recognition changes. Common Skill requirements, Workflow gates, Assessment judgments, Validation proof criteria, published skill methods and record representation remain their existing dependencies. No independent approval, final Verify, commit or publication is claimed.

## Project Support decomposition: authorized execution outline

The user selected Project Foundations (Vision, Constitution, Project Map), Discovery (Explore, Research), Learning and Delivery Handoff and authorized the proposed directory layout. This is a subsequent isolated model-authoring correction; previous recorded subjects and results above retain their original scope.

Capture current sources; extract complete specialist behavior, vocabulary and output contracts into the selected nine documents; define the two composition owners and representative acceptance/architecture views; reconcile Skill/System hierarchy, live links and exact model-path admission; extend existing positive-path and unknown-path checks; run focused validation and full selected local CI; record preservation, current subject identities and outcomes here. Keep public invocations, permission boundaries and independently owned Workflow/Assessment contracts unchanged. The CI-maintenance repair exception stays with Implementation; Delivery Handoff references it. Shared Skill policy stays shared. Historical records and earlier subject sets are not rewritten.

## Project Support decomposition result and preservation

Nine new model documents replace the former Project Support grouping. Project Foundations composes Vision, Constitution and Project Map; Discovery composes Explore and Research; Learning and Delivery Handoff own Learn and PR respectively. All twenty-four declared model paths are explicit in the repository catalog. Public skill names, resources and workflow stages are unchanged.

| Former Skill section | Current owner and treatment |
| --- | --- |
| Project Support capability inventory | Skill now delegates four distinct owners; System and Skill diagrams/inventories name their composition. |
| Vision boundary, vocabulary and results | `project-foundations/vision.md`; full existing boundary and vocabulary retained, with ordered-write runtime view. |
| Project Map boundary, vocabulary and results | `project-foundations/project-map.md`; full existing boundary and vocabulary retained, with root/area registration and audit distinctions. |
| Learn boundary, confirmation vocabulary and results | `learning.md`; full session, topic, route and result-recording obligations retained, with explicit confirmation gating. |
| PR boundary, vocabulary and results | `delivery-handoff.md`; full verified-identity, authority, retry and external-observation obligations retained. Shared governed-signal vocabulary remains in Skill. |
| Constitution, Explore and Research summary | Their separate child models retain purpose and placement boundaries; existing published methods substantiate engineering detail. Common Skill policy remains shared. |
| Bounded PR CI repair | Remains byte-identical in Skill’s Implementation responsibility; Delivery Handoff references it without expanding authority. |
| SKL-SR-27/29 | Stable IDs retained; replace the former group name with the four current owners. Other Skill requirement rows are unchanged. |

Direct preservation checks confirmed complete extracted Vision, Project Map, Learn and PR boundary prose and every moved public-vocabulary row. The existing result paragraphs were distributed to their respective owners. The common discovery-support family declaration remains in Skill. Existing standalone/incidental support distinctions and no-adoption limits are explicit in Discovery and its children. Git tracking follows governing project authority; the Constitution’s optional Git boundary is not overridden by a generic skill’s artifact-output wording. No public skill source was changed by this decomposition.

`python scripts/validate-boundary-first.py --check` passed all 24 current model documents and selected examples after the final prose updates. `python scripts/test-boundary-first-validation.py` passed all 61 tests; its independent positive inventory now includes all nine new paths and its unknown-path negatives cover both new child directories. All 57 model-owned example files are unchanged. The final local-link audit resolved 674 links; `git diff --check` passed.

### Project Support decomposition subject set

These identities cover the full current model composition and the two changed catalog/test consumers. Previous subject sets and evidence above retain their original meaning; this authoring does not retarget their approval.

| Subject | Identity |
| --- | --- |
| `docs/design/system.md` | `sha256:bce5d61c3fb770fd0b2346f6ab950bd9caf18d4a617cc9709bc7ca3a6d74d9d1` |
| `docs/design/skill/authoring/authoring.md` | `sha256:4b75b6738fb94bac2272395dfeffce9eaa760e0fdb8ac241c9e90027cd3e7071` |
| `docs/design/skill/authoring/proposal.md` | `sha256:c476948880f22deaf4f332f6a0e2af1e6eba378869931b86bba501866a0b8bdf` |
| `docs/design/skill/authoring/design.md` | `sha256:0f7e41a1de83d542d9291a842876ae7c75ebb214242cca18ee825f539f52b778` |
| `docs/design/skill/authoring/plan.md` | `sha256:5ea24213d164d301ed4690f3647bbb98d8f5709bf10af03c3efbdcad08ef9826` |
| `docs/design/skill/project-foundations/project-foundations.md` | `sha256:194488c1140a1d993b3f12abb1b95620fd2f393d875f4729aeb1273b8e180208` |
| `docs/design/skill/project-foundations/vision.md` | `sha256:df22fcfba261aa582ae0985fd0b34308fd66939ff3cf8c2387600436a2dd6306` |
| `docs/design/skill/project-foundations/constitution.md` | `sha256:4e0183e9896ab301f1777410b9a3665d5d4ef392dbd4ed010b27397113c2e3ac` |
| `docs/design/skill/project-foundations/project-map.md` | `sha256:800906693355cd9585df023e9383de6e8aafe9095c289bdd5a7a155d6e96199b` |
| `docs/design/skill/discovery/discovery.md` | `sha256:60b82391fad61a27b6187b1728264d85ae6068fadc252eb249449b462d3d6a86` |
| `docs/design/skill/discovery/explore.md` | `sha256:f2a75649d9770c01014e119324b66939272a8f8b3ff58bb3ac3d9e2e744c5095` |
| `docs/design/skill/discovery/research.md` | `sha256:3b2ac4c5adcf66d5d6bebcff4e3dc73d4ecca16855c735c5766f7676b5e214df` |
| `docs/design/skill/learning.md` | `sha256:592fd8c1e4bbdbd15ea1c1f4bb3a1977a1ffe7738dbbe989bfb262cd71967cb7` |
| `docs/design/skill/delivery-handoff.md` | `sha256:afc1c0e613582e71b88cf5169d1d1b0dce9a8c84ba8348c39b324235c9435fce` |
| `docs/design/skill/workflow.md` | `sha256:ed5380294c163077a289f3566e041b8d580f7217b15756f961aba181400a757d` |
| `docs/design/skill/assessment.md` | `sha256:af968c5583c27abb95e9a2b2ac0293deba30c522e72480c6f9aa05066659ae19` |
| `docs/design/cli/records.md` | `sha256:8bcdb2872f9b6887212264493f1421a58b91b9b7ab9d12bcd240bcd5a9b5f966` |
| `docs/design/cli/installation.md` | `sha256:5320ca604afb4e0a224317e80e9907c2fc42b78f5ab6322bc81b45d8687d6574` |
| `docs/design/engineering/validation.md` | `sha256:52b528ec2c16f6443ab26cb10adf291de7741c0dbddf188599de8fa9a34c4dd5` |
| `docs/design/engineering/packaging.md` | `sha256:0c9f56b406cf083682f7c353387dbcc2eff59cf4039aaafc7c6e01aa657de8d2` |
| `docs/design/engineering/release.md` | `sha256:f07bd1f3bb74889d4eb6dcba223c9dc1374d7843775cfc027c72de6b0bff511a` |
| `docs/design/skill/skill.md` | `sha256:cc0f83008c3f84841d4f0b05a221d38cd60db42abf344cb88bbe65e4ad572540` |
| `docs/design/cli/cli.md` | `sha256:12d601e7db59aa57380ad4a2d635ec1ba05ba6ac2477cd822688898a6096e504` |
| `docs/design/engineering/engineering.md` | `sha256:a29cdbaadcdbaf038a01b476bf290e6d1702da2e89faba230b82be120e6a10f8` |
| `scripts/model_layout.py` | `sha256:8b518a76baf1d9b8e1aa1e44c64b7419053d18df4c7ea775f2eaccc62fe6062f` |
| `scripts/test-boundary-first-validation.py` | `sha256:67059649499b72aef9e9c34dac61fc855bdcecbdf1ea0da2e7fdfa22242a52fa` |

### Final Project Support decomposition validation and handoff

`bash scripts/ci.sh --mode local` passed with 1020 passing check/case rows across `adapters.regression`, `boundary_first.regression`, `boundary_first.validate`, `change_metadata.regression`, `current_records.validate`, `documentation_prose.enforce`, `guide_system.validate`, `markdown_readability.validate`, `model.validate`, `readme.validate`, `readme.vision_markers`, `record_store.schema`, `rigorloop_cli.test`, `selector.regression`, `skills.regression`, `skills.validate`, `validation_execution.regression`. This covers the full current working tree, including the earlier refinements, current record validation, model admission, deleted-path routing, selector regressions, skill/adapter behavior and validation execution. Final model and boundary checks were also rerun after the last Discovery and Workflow prose changes. All 57 example files were verified byte-identical to the retained Git baseline (the scoped temporary snapshot contained only the Markdown examples). Final subject identities were unchanged after CI, and diff hygiene passed.

The package is authored and mechanically checked. Independent Design Review should assess the four-way responsibility split, the two composed parents and their five children, the two single-document specialists, the two preserved SKL owner-name amendments, affected System/Skill/Workflow relationships and exact path admission. Earlier approvals retain their own subjects. No independent review approval, final Verify, hosted CI, commit or publication is claimed.

## Test-design alignment advisory audit

The user asked whether the existing test scripts fit the repository’s test design. This is a bounded advisory audit against Validation TEST-SR-01–14 and VAL-SR-01–10, not independent approval of the suite or of the author’s preceding model changes. No production source, test or selection rule was edited during this audit.

Inventory: eighteen Python test files under scripts comprise fourteen catalogued entrypoints and four release-test modules imported by `test-release-transaction.py` (imports near lines 1900–1905). Static AST enumeration found 925 test methods across these files; this is not an execution total and excludes Node tests and runtime expansion. The four release modules are not orphan suites. Multiple adapter catalog entries intentionally select different cases; overlapping identities need executor composition, not duplicate-file deletion.

Representative positive evidence: archive resource completeness uses an independent filesystem/byte oracle in `test-adapter-distribution.py`; npm publication tests exercise actual pack and installation boundaries; retained-query tests prove rejection without reading or changing archival input; execution composition tests observe a real subprocess marker to ensure overlapping checks run once while retaining failed-gate behavior. Skill tests explicitly distinguish structural validity from semantic quality: a structurally valid ambiguous skill is deliberately accepted, and semantic review remains separately owned. These protections fit Validation’s boundary and oracle criteria; no redundant-removal finding is established from size or naming alone.

### AUD-TEST-01: extracted model owners escape an existing guide-authority check

`validate-guide-system.py` enumerates only AGENTS, Constitution, README, project map, Workflow, Assessment and Skill as current surfaces for ROUTE-GUIDE-006. Its regression fixture likewise lists only those model owners. The recent extraction moved normative behavior into additional model documents, but this consumer was not reconciled. This is a TEST-SR-10 protection gap in the preceding refinement despite passing CI.

Reproduction used the existing RouteGuideValidatorTests fixture in separate temporary repositories. Baseline validation passed in each. Appending `Use docs/workflows.md.` to Skill produced ROUTE-GUIDE-006 and rejection. Appending the same instruction to copied current Plan, Research or Delivery Handoff documents produced no finding and validation still passed. The real working-tree files were not mutated. Required correction: preserve this forbidden-authority check across the explicitly declared current model owners, using the shared model catalog or an equally justified complete population, and extend negative fixtures for extracted owners while retaining current privacy/path and historical-scope boundaries. Reassess selection dependencies if the validator starts consuming the catalog.

### AUD-TEST-02: policy-admission assertion cannot detect contradictory policy

`test_discovery_shared_policy_is_admitted_by_skill_contract` checks only that Skill contains `discovery-support`. The isolated test still passes when its Path.read_text result is replaced with `The discovery-support policy is forbidden and MUST NOT be admitted.` This counterexample demonstrates that it is a name-presence guard, not sufficient proof of admission. It also explains why a correct prose relocation previously caused a failure even though the test would miss an actual reversal of meaning.

Required correction: give the structural test a narrowly truthful claim and preserve declared resource/projection integrity with meaningful negative tests at that boundary. Admission meaning requires independent semantic review or an explicitly structured contract if one is separately justified; do not build an arbitrary prose parser or remove the only protection without a replacement assessment. This is a TEST-SR-04/05 claim-sensitivity concern, not evidence that every assertIn or source-level guard is useless.

### Audit validation and limits

Ran a temporary non-mutating sensitivity script for the four guide placements and the policy counterexample; outcomes above were observed. Ran `python scripts/test-guide-system-validator.py`: all 11 existing tests passed despite AUD-TEST-01. Ran `python scripts/test-validation-execution.py CompositionTests.test_canonical_overlap_runs_once_with_reasons_and_failure_gate`: its one test passed. No broad rerun was needed for this read-only audit; prior CI remains execution evidence for its original subjects, not proof against these counterexamples.

The audit inventories all eighteen files and samples representative checks; it does not claim complete per-case traceability, mutation adequacy, agent behavior coverage, release qualification or independence certification. Recommended maintenance is to repair the demonstrated population gap first, then classify source-text assertions by their actual protection and strengthen those whose claims exceed it. Large selector/skill suites may be organized by responsibility for navigation, but size alone does not justify deleting or consolidating their cases.

Audit subjects:

| Subject | Identity |
| --- | --- |
| `docs/design/engineering/validation.md` | `sha256:52b528ec2c16f6443ab26cb10adf291de7741c0dbddf188599de8fa9a34c4dd5` |
| `scripts/validate-guide-system.py` | `sha256:f933f576b5d827b54065fc2fe84481e985974169f40091b52f0f17aee8807e5b` |
| `scripts/test-guide-system-validator.py` | `sha256:f5a7941ed35cad94592cc85a1082e78f612c1739bb8d41f31a2ee9aa2c21aa08` |
| `scripts/test-skill-validator.py` | `sha256:a91a45ca90e098a5fd8f2301111cc3a9ddf5e05f0012344aee9806c6e8ac4cc9` |
| `scripts/validation_selection.py` | `sha256:686e24be4a47ddf34be53eaad4126a002eae773f9312c05bda8ce4cda2df8fea` |
| `scripts/test-validation-execution.py` | `sha256:f3928ff16d2ab33fb68ad8054a5884e7f4cbabf90d8225f46486d5424a5c9f57` |
| `scripts/test-adapter-distribution.py` | `sha256:c86259d3dc825303f5307f2d75048364e665177f27e7817c52f5b9ed94802ac9` |
| `scripts/test-release-transaction.py` | `sha256:6737ec7dedec8aebebb4ce938660e8d195054bc5921ec84743ecd1a958d3a4b2` |
| `scripts/test-npm-package-publication.py` | `sha256:aa69580c188ce34b4164413390fd0e683f3843b8a7b103d6432d9967ca00f376` |

## Test-group coverage audit and correction outline

The user authorized the proposed test-group audit and correction of demonstrated gaps. Inventory Python and package-native test populations, map coherent groups to current Design obligations and observation boundaries, distinguish structural protection from semantic/agent or external evidence, and record gaps/dispositions here. Do not require a new per-test ledger or remove unknown protection.

Correction sequence: add failing regression proof for extracted model-authority checks and catalog-consumer selection; make the guide validator consume the declared model inventory; reconcile selection; preserve history/non-model scope; narrow the policy-name assertion’s claim without inventing a prose-semantics parser or losing its structural guard; run focused tests followed by repository-selected checks. AUD-TEST-01 is a reproduced consumer-coverage defect under TEST-SR-10; AUD-TEST-02 is a claim-sensitivity correction under TEST-SR-04/05. This isolated fix does not grant independent review approval or change public runtime behavior.

## Repository test-group coverage map

Scope: all eighteen Python test files under scripts and all twenty-two package-native `.test.js` files. Each Python test class with directly declared test methods is indexed below; Node files are coherent groups, including the dynamically registered interaction scenarios. Helper classes, launchers and fixture builders are supporting dependencies rather than additional test obligations. Group-level ownership and observation boundaries satisfy the useful mapping level in TEST-SR-01; this is not a claim of exhaustive per-requirement proof or per-case mutation coverage.

### Findings and maintenance decisions

- AUD-TEST-01: corrected the real lost-consumer protection. Three extracted-owner subcases failed before the production change; the model-catalog selection regression also failed. The validator now checks all declared current model paths while leaving historical and undeclared example documents outside this authority scan. Catalog edits select live guide validation and its regressions. Existing model validation retains target existence/path safety; this change does not create another model scanner.
- AUD-TEST-02: corrected the test’s overclaim by renaming it to `test_skill_contract_names_discovery_shared_policy` and documenting that it is only a structural name-presence guard. Its assertion remains intact. It still cannot detect semantically contrary prose; independent review remains necessary and this correction does not claim added semantic coverage. Existing resource parity and malformed/missing-resource tests provide separate executable protection.
- No group is selected for deletion. Size, historical class names, overlapping filenames and multiple catalog selections are not sufficient evidence of redundant protection. Real overlap execution is protected by the executor’s marker-based composition test.
- Claim-limited coverage: specialist source-text/profile tests establish exposure and packaging of instructions, not actual agent compliance with Plan initialization authority, Learn confirmation, PR mutation limits, Vision sequencing or reviewer independence. Their owning Plan/assessment must supply representative walkthrough or other adequate semantic evidence for a behavioral change. This audit did not reassess historical walkthrough applicability.
- Release/provider fixtures prove implementation behavior under controlled outcomes, not actual current hosted approvals or publication. Current external qualification remains a separate Release obligation.

### Python entrypoints and observation boundaries

| File | Current owner | Protected behavior | Observation boundary / limit | Invocation |
| --- | --- | --- | --- | --- |
| `scripts/release_candidate_tests.py` | `docs/design/engineering/release.md` | Candidate identities, integrity, CI-only qualification and actual build orchestration | In-process candidate logic plus temporary Git/package/process integration; external publication is outside this proof. | Imported by `release_transaction.regression` |
| `scripts/release_coordination_tests.py` | `docs/design/engineering/release.md` | Ordered coordination, candidate/approval boundaries and recoverable outcomes | Coordinator with fixture hosted services; package fixtures retain actual construction where invoked. | Imported by `release_transaction.regression` |
| `scripts/release_evidence_tests.py` | `docs/design/engineering/release.md` | Required evidence representation, status/identity consistency and unknown-value rejection | Validator calls on synthetic evidence; does not establish that claimed hosted events occurred. | Imported by `release_transaction.regression` |
| `scripts/release_execution_tests.py` | `docs/design/engineering/release.md` | Exact approval binding, immutable evidence and bounded publication/recovery | Actual execution/persistence logic with fixture providers; no live publication qualification. | Imported by `release_transaction.regression` |
| `scripts/test-adapter-distribution.py` | `docs/design/engineering/packaging.md` | Complete supported archives, independent bytes, metadata, malformed resources and safe output paths | Real archive/filesystem and command boundaries with independent inventories; focused and full catalog selections differ. | `adapters.regression`, `adapters.drift`, `adapters.validate`, `adapters.full_regression` |
| `scripts/test-boundary-first-reference.py` | `docs/design/engineering/packaging.md` | Portable boundary resource projection, containment and byte parity | Actual projection/filesystem and reference-validator behavior; Design owns the retained feature format. | `boundary_first.reference_regression` |
| `scripts/test-boundary-first-validation.py` | `docs/design/skill/authoring/design.md` | Required model/legacy structure, invalid inputs, declared paths, examples and review-handoff limits | Actual validator calls and CLI output over positive/negative temporary documents; no semantic model approval. | `boundary_first.regression` |
| `scripts/test-change-metadata-validator.py` | `docs/design/cli/records.md` | Current stored shapes and registered metadata checked without importing retired engines | Record validator/command fixtures; CLI safety and evidence truth have separate checks. | `change_metadata.regression` |
| `scripts/test-documentation-prose-validator.py` | `docs/design/engineering/validation.md` | Documentation prose scope, formatting diagnostics and enforcement modes | Mutated temporary prose and command outcomes; does not measure readability or usability. | `documentation_prose.regression` |
| `scripts/test-governed-lifecycle-cli-validator.py` | `docs/design/cli/cli.md` | Discovery and exact Git-snapshot scope, ignored archives and no-write validation | Temporary repositories and actual wrapper/subprocess behavior; recorded status is not semantic readiness. | `governed_lifecycle_cli_wrapper.test` |
| `scripts/test-guide-system-validator.py` | `docs/design/skill/workflow.md` | Current navigation and rejection of retired authority across current owners | Mutated temporary repository fixtures; extracted-owner negative and historical exclusions added in this correction. | `guide_system.regression` |
| `scripts/test-markdown-readability-validator.py` | `docs/design/engineering/validation.md` | Declared Markdown-format scope, structural rendering constraints and diagnostics | Temporary Markdown/validator fixtures; no assessment of user understanding. | `markdown_readability.regression` |
| `scripts/test-npm-package-publication.py` | `docs/design/engineering/packaging.md` | Publishable package inventory, real packed installation and installed CLI safety | Actual npm pack/install and installed binary in temporary roots; CLI/Installation are integrated consumers. | `npm_package_publication.test` |
| `scripts/test-query-change-record.py` | `docs/design/cli/cli.md` | Retired helper rejection without decoding/reading/mutating archives | Real subprocess plus read-forbidden guard; small scope is intentional and useful. | `change_record_query.regression` |
| `scripts/test-release-transaction.py` | `docs/design/engineering/release.md` | Profile/tag/inventory/literal-baseline/preparation/preflight/evidence consistency and history preservation | Temporary Git/package/filesystem inputs and controlled providers; four imported test modules remain discovered. | `release_transaction.regression` |
| `scripts/test-select-validation.py` | `docs/design/engineering/validation.md` | Known/unknown/deleted/renamed input selection, actual callers, output and phase gates | Pure selector outputs plus real Git/CLI/wrapper integration. A check-selection assertion proves routing, not execution. | `selector.regression` |
| `scripts/test-skill-validator.py` | `docs/design/skill/skill.md` | Canonical package structure, mapped resources, explicit scopes and current specialist contract exposure | Mixed fixture-validator, filesystem parity and source-text guards; semantic behavior is not implied by literal presence. | `skills.regression` |
| `scripts/test-validation-execution.py` | `docs/design/engineering/validation.md` | Independent discovery, bounded execution, statuses, timeout/reaping, dependency gates and overlap | Actual subprocesses and injected failures/markers; public services remain controlled fixtures. | `validation_execution.regression` |

### Python coherent-group index

The table above defines each file’s protected behavior and boundary. This index makes every directly declared case group findable at the applicable owner; mixed large classes still need targeted case inspection for a particular requirement. Counts are source organization only, not execution evidence or coverage percentages.

| File / group | Direct methods | Governing owner | Disposition |
| --- | --- | --- | --- |
| `release_candidate_tests.py::ReleaseCandidateTests` | 19 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_candidate_tests.py::ReleaseCandidateIntegrationTests` | 2 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_candidate_tests.py::CurrentSourceQualificationTests` | 6 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_coordination_tests.py::ReleaseCoordinationTests` | 9 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_evidence_tests.py::ReleaseEvidenceTests` | 15 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_execution_tests.py::ReleaseApprovalTests` | 5 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_execution_tests.py::ReleaseEvidenceStoreTests` | 2 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `release_execution_tests.py::ReleaseExecutorTests` | 19 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-adapter-distribution.py::AdapterDistributionTests` | 96 | `docs/design/engineering/packaging.md` | Retain; observed boundary described above |
| `test-boundary-first-reference.py::BoundaryFirstReferenceTests` | 28 | `docs/design/engineering/packaging.md` | Retain; observed boundary described above |
| `test-boundary-first-validation.py::BoundaryFirstStructuralTests` | 30 | `docs/design/skill/authoring/design.md` | Retain; observed boundary described above |
| `test-boundary-first-validation.py::BoundaryFirstActivationTests` | 8 | `docs/design/skill/authoring/design.md` | Retain; observed boundary described above |
| `test-boundary-first-validation.py::AdoptionReviewHandoffTests` | 7 | `docs/design/skill/assessment.md` | Retain; observed boundary described above |
| `test-boundary-first-validation.py::ModelRecordTests` | 12 | `docs/design/skill/authoring/design.md` | Retain; observed boundary described above |
| `test-boundary-first-validation.py::CurrentBoundaryCommandTests` | 4 | `docs/design/skill/authoring/design.md` | Retain; observed boundary described above |
| `test-change-metadata-validator.py::ChangeMetadataValidatorFixtureTests` | 1 | `docs/design/cli/records.md` | Retain; observed boundary described above |
| `test-change-metadata-validator.py::ExplicitRecordingMetadataTests` | 6 | `docs/design/cli/records.md` | Retain; observed boundary described above |
| `test-documentation-prose-validator.py::DocumentationProseValidatorTests` | 14 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-governed-lifecycle-cli-validator.py::CurrentRecordDiscoveryTests` | 3 | `docs/design/cli/cli.md` | Retain; observed boundary described above |
| `test-governed-lifecycle-cli-validator.py::CurrentRecordSnapshotTests` | 3 | `docs/design/cli/cli.md` | Retain; observed boundary described above |
| `test-guide-system-validator.py::RouteGuideValidatorTests` | 13 | `docs/design/skill/workflow.md` | Retain and strengthen; extracted-owner/historical-scope proof added |
| `test-markdown-readability-validator.py::MarkdownReadabilityValidatorTests` | 7 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-npm-package-publication.py::NpmPackagePublicationTests` | 7 | `docs/design/engineering/packaging.md` | Retain; observed boundary described above |
| `test-query-change-record.py::RetiredQueryTests` | 2 | `docs/design/cli/cli.md` | Retain; observed boundary described above |
| `test-release-transaction.py::HistoricalReleaseReaderTests` | 2 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::TrustedReleaseTagIdentityTests` | 5 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::DeterministicReleaseGateTests` | 1 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::ReleaseProfileTests` | 12 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::ReleaseSurfaceInventoryTests` | 5 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::LiteralAuditBaselineTests` | 7 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::PrepareReleaseTests` | 25 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::ReleasePreflightTests` | 16 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::ReleaseGateParityAndTimingTests` | 9 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-release-transaction.py::PublishedEvidenceCloseoutTests` | 17 | `docs/design/engineering/release.md` | Retain; observed boundary described above |
| `test-select-validation.py::ScriptOutputContractTests` | 10 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-select-validation.py::ValidationSelectionTests` | 167 | `docs/design/engineering/validation.md` | Retain and strengthen; new catalog consumer included |
| `test-skill-validator.py::SkillValidatorFixtureTests` | 151 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::MarkdownReadabilityGuidanceTests` | 4 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RetainedSkillAuthorityTests` | 4 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RouteSkillCutoverContractTests` | 7 | `docs/design/skill/workflow.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::VerifySkillSimplificationContractTests` | 5 | `docs/design/skill/assessment.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::PRSkillSimplificationTests` | 14 | `docs/design/skill/delivery-handoff.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::PlanSkillSimplificationContractTests` | 3 | `docs/design/skill/authoring/plan.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ProposalSkillSimplificationTests` | 6 | `docs/design/skill/authoring/proposal.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::UnifiedDesignResourceTests` | 5 | `docs/design/skill/authoring/design.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::VisionSkillProgressiveDisclosureTests` | 5 | `docs/design/skill/project-foundations/vision.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ExplainChangeSkillRetirementTests` | 1 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::LearnSkillSimplificationTests` | 5 | `docs/design/skill/learning.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ProjectMapSkillSimplificationTests` | 6 | `docs/design/skill/project-foundations/project-map.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::CiMaintenanceSkillSimplificationTests` | 8 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::BugfixSkillSimplificationTests` | 10 | `docs/design/skill/skill.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ConsolidatedReviewGateSkillContractTests` | 4 | `docs/design/skill/assessment.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RequirementDeliveryModelM1Tests` | 3 | `docs/design/skill/authoring/design.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RequirementDeliveryModelM2Tests` | 2 | `docs/design/skill/assessment.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ReviewCloseoutResourceTests` | 3 | `docs/design/skill/assessment.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RequirementDeliveryModelM3Tests` | 3 | `docs/design/skill/authoring/design.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RetireStandaloneTestSpecM3Tests` | 6 | `docs/design/skill/authoring/plan.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RetireStandaloneTestSpecM4Tests` | 3 | `docs/design/engineering/packaging.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::FinalVerificationPackageParityM4Tests` | 2 | `docs/design/engineering/packaging.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::RetireStandaloneTestSpecM5Tests` | 3 | `docs/design/engineering/validation.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::OptionalDiscoverySkillContractTests` | 10 | `docs/design/skill/discovery/discovery.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::ExplicitRecordingGuidanceTests` | 4 | `docs/design/skill/workflow.md` | Retain; claim-limited structural/resource evidence |
| `test-skill-validator.py::TestPolicyResourceTests` | 3 | `docs/design/engineering/validation.md` | Retain; claim-limited structural/resource evidence |
| `test-validation-execution.py::CaseAdapterTests` | 11 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-validation-execution.py::ExecutionTests` | 21 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-validation-execution.py::NodeCaseAdapterTests` | 5 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-validation-execution.py::CatalogTests` | 4 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |
| `test-validation-execution.py::CompositionTests` | 11 | `docs/design/engineering/validation.md` | Retain; observed boundary described above |

### Package-native test groups

All groups below are selected through `rigorloop_cli.test` / `npm test` (`node --test "test/**/*.test.js"`); `record-retirement.test.js` also has its bounded catalog check. Current and v3-labelled files protect different public, internal, stored and transport boundaries; historical-looking names alone do not establish redundancy. Integration helpers and launchers are included by these actual consumers.

| File | Current owner | Protected behavior / boundary | Disposition and limitation |
| --- | --- | --- | --- |
| `packages/rigorloop/test/cli-invocation-observability.test.js` | `docs/design/cli/cli.md` | Command-family terminal severity/status, invocation recording and no-write/diagnostic constraints | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/cli-observability.test.js` | `docs/design/cli/cli.md` | Logging configuration, privacy, safe paths, locking and platform behavior | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/cli.test.js` | `docs/design/cli/cli.md` | Public command envelope, installed/acquisition boundaries and error compatibility | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/installer-replacement.test.js` | `docs/design/cli/installation.md` | Whole-candidate conflicts, explicit force, races, path safety and partial/retry outcomes | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-retirement.test.js` | `docs/design/cli/records.md` | Unsupported stored-format rejection and archival exclusion without mutation | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-adoption.test.js` | `docs/design/cli/cli.md` | Complete public targeted correction and receipt with realistic dense evidence | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-cli.test.js` | `docs/design/cli/cli.md` | Advanced public inspect/check/record behavior, bounded receipts and safe diagnostics | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-contract.test.js` | `docs/design/cli/records.md` | Inspection snapshots, absent/missing sidecars, serialization and diagnostic representation | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-format.test.js` | `docs/design/cli/records.md` | Current fixtures/templates, closed vocabulary and stored consistency constraints | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-interactions.test.js` | `docs/design/cli/cli.md` | Equivalent advanced/targeted interactions and basis/checkpoints; dynamically registered SCENARIOS | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-model-examples.test.js` | `docs/design/skill/authoring/design.md` | Executable example/owner validity and observation-identity relationships; not narrative adequacy | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-persistence.test.js` | `docs/design/cli/cli.md` | Exact bytes, stale read/revision conflicts, lost response, writer exclusion and recovery | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-queries.test.js` | `docs/design/cli/cli.md` | Selectors, pagination/budgets, text/JSON representation and complete/partial result scope | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-targeted.test.js` | `docs/design/cli/cli.md` | Targeted edits, preview, batches, collisions and preserved neighboring fields/bytes | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-v3-adoption.test.js` | `docs/design/cli/records.md` | Current v3 creation through advanced and targeted entrypoints with independent transport versions | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-v3-contract.test.js` | `docs/design/cli/records.md` | V3 fields, explanation collections and stored contracts without historical conversion | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-v3-mutations.test.js` | `docs/design/cli/records.md` | V3 edits, no-op/neighbor preservation and invalid explanation member rejection | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-v3-persistence.test.js` | `docs/design/cli/cli.md` | V3 creation/persistence and independently versioned result envelopes | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-v3-reads.test.js` | `docs/design/cli/cli.md` | V3 projection identity/applicability and unknown selectors before repository access | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/record-store-workflow.test.js` | `docs/design/skill/workflow.md` | Actor-selected restriction/reopening/failure/reassessment through actual record mechanics; actor independence not authenticated | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/result-renderer.test.js` | `docs/design/cli/cli.md` | Closed output formats, concise rendering, budgets and diagnostic result envelopes | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |
| `packages/rigorloop/test/workflow-context.test.js` | `docs/design/cli/cli.md` | Factual candidate discovery/current revisions, scoped selection and omissions without stage inference | Retain. Synthetic stores/actors and controlled environments do not authenticate human judgment or live external state. |

### Requirement-to-proof limits and next allocation

The owned Requirements and acceptance scenarios at each linked model are the source of expected outcomes; fixtures, names and existing output are not substitute authority. This inventory establishes test-group ownership and known observation boundaries, not that every requirement has sufficient current evidence. For a concrete behavioral change, the owning delivery plan must map all affected requirements and material hazards to the groups above, necessary new proof, or independent review/manual evidence with explicit prerequisites and limitations.

The next justified maintenance candidates are source-text checks in `PlanSkillSimplificationContractTests`, `RequirementDeliveryModelM1Tests/M2Tests`, and `OptionalDiscoverySkillContractTests`: assess whether each literal represents a required package interface or merely historical wording, while preserving resource-loading, state/authority and output protection. No semantic parser, automated-agent harness, directory migration, blanket parameterization or removal quota is selected by this audit. Integration and lower-level cases remain separate when they expose different failures.

### Coverage-audit correction evidence

The extracted-owner regression failed in all three subcases before the guide consumer correction; the model-inventory selection regression also failed before its dependency correction. After correction, all 13 guide regression tests passed, the focused model-inventory selection test passed, and the renamed policy-name test passed. Live `python scripts/validate-guide-system.py` passed. Temporary counterexample probes now reject the retired-authority instruction in Skill, Plan, Research and Delivery Handoff. The historical/undeclared-document regression preserves the current-owner boundary.

The policy-name counterexample still passes deliberately: the assertion establishes name presence only. The test name and comment now state that limited claim; policy admission semantics remain independent review work. No test group was removed.

`bash scripts/ci.sh --mode local` completed successfully with 1033 passed check/case rows across 18 selected check families; boundary scope passed. These are actual execution rows, not the static inventory counts. Full local output is at `/tmp/coverage-ci.log` (ephemeral). No hosted CI, independent review or external publication is claimed.

Correction source identities (SHA-256 after successful local CI):

| Path | SHA-256 |
| --- | --- |
| `scripts/validate-guide-system.py` | `2024ae62d69017d582d18cfb611b17d79a34d2272452413c24ee9b5aa49c4984` |
| `scripts/test-guide-system-validator.py` | `439c51ba1e2569b0d8ee3955e821dfc49654b3dda6cb796fdacd30869116ad96` |
| `scripts/validation_selection.py` | `1b033fd99ee2011025cce7a2e0ab8a3d54d99e09b043720187557ca953e9af18` |
| `scripts/test-select-validation.py` | `f1346bcba6dca3d84814a1554fbd2c665d31e43b0ef3a01bd2d52d97dffa14e2` |
| `scripts/test-skill-validator.py` | `5a9af830da3f40d7cfac54e5933590ea3b772a19984f6b8744952f9eeeaa8612` |

This records author validation, not independent approval. The coverage map supports targeted follow-up decisions; it does not claim exhaustive requirement coverage or case-by-case redundancy proof.

## Validation refactor design refinement

The user authorized starting the validation refactor after selecting proportionate evidence and ownership-based test organization. This slice authors the existing Validation and System Designs; it does not adopt the previous cleanup records as approval of these new subjects. Earlier uncommitted changes and judgments are preserved. Workflow-context discovery succeeded using `node packages/rigorloop/dist/bin/rigorloop.js workflow-context --format json`; no new governed change identity was selected or manufactured.

Validation TEST-SR-15–17 define evidence choice, semantic assessment and coordinated test relocation. Existing TEST-SR obligations and all execution requirements remain. VAL-DEC-09 records the choice; the overview exposes the non-executable evidence path, and identity/authority and migration scenarios cover the new obligations. System links the source placement convention to Validation's concrete layout. The existing Context, Building Block, Runtime and Deployment views remain necessary: source placement adds no service or process, and review authority stays outside the executor.

### Affected consumers and migration scope

| Subject or consumer | Disposition |
| --- | --- |
| Validation and System | Changed owning contracts; independent review must assess both exact subjects together. |
| Assessment, Workflow, Plan and Constitution | Relied-on unchanged authority: existing independent reviews, concrete delivery allocation and evidence disposition already apply. Human PR review is conditional and does not replace earlier gates. No new semantic gate or skill change is selected. |
| `scripts/test-*.py`, imported release support modules and test-only helpers | Implementation scope: inventory by actual owning capability, move in bounded coherent groups and preserve independent discovery. Existing audit maps the population; it is not per-case deletion approval. |
| `scripts/validation_selection.py`, execution adapters, direct shell/package/release callers | Implementation scope: reconcile commands, path selection, root resolution and rerun commands; retain canonical IDs where the observation is unchanged. Source inspection confirms hard-coded Python test paths in the current catalog. |
| Existing fixture roots and inline/temporary generators | Implementation scope: identify exclusive/shared consumers, migrate exclusive resources with their group and retain isolation and invalid-input detection. Production helpers cannot acquire test-source dependencies. |
| `packages/rigorloop/test/` | Package-owned location retained; only actual cross-boundary dependencies need reconciliation. |
| CONTRIBUTING and project map | Contributor commands still refer to supported `ci.sh` modes, so no premature path change. Reconcile navigation and observed map during migration; the map was not relied on for current source placement. |
| Published skills, packaging/release contracts and model examples | No changed public skill resource, release permission, artifact shape or example behavior. Actual callers and packaging tests remain migration consumers; their substantive protection stays required. |

The migration handoff is to independent Design Review, then concrete delivery allocation under existing authority. This is a reviewable design slice, not an approved execution plan or a completed repository refactor. No test files have moved and no cases have been removed in this slice. The design skill's downstream requirement and Constitution's pre-implementation Design/delivery review rules apply; author validation cannot supply independent approval.

### Author validation and identities

`python scripts/validate-boundary-first.py --check --path docs/design/engineering/validation.md --path docs/design/system.md` passed structure/reference validation. `python scripts/validate-documentation-prose.py --mode enforce --path docs/design/engineering/validation.md --path docs/design/system.md` passed with zero errors and warnings. `git diff --check` passed. These are mechanical checks, not semantic approval.

- `docs/design/engineering/validation.md`: SHA-256 `26ade9487d6db3f6c190b3f073af506431c6b47e1d7aab0f5b2c72cf897286f1`.
- `docs/design/system.md`: SHA-256 `45994453b7977569cfcf166f8543b492186b0932a1e8798c9c238c5e7e8623bc`.

Selected validation: `bash scripts/ci.sh --mode explicit --path docs/design/engineering/validation.md --path docs/design/system.md` exited successfully with 489 passed check/case rows across 5 check families. Local Markdown target existence checks found no missing targets in either changed Design. Exact before/after subject inspection succeeded. No hosted CI or independent review is claimed.

### Branch and independent review handoff

Created `refactor/validation-organization` from `main` at the user's request, preserving the existing working tree. The branch contains preceding Design refinements and audit corrections as well as this validation refinement; branch creation does not isolate or approve those changes. No commit or external publication was performed.

The next review assesses the exact complete Validation and System members below, with particular attention to TEST-SR-15–17, VAL-DEC-09, evidence selection, source placement, migration preservation and their interactions with Assessment, Workflow, Plan and Constitution. Prior author validation supplies mechanical evidence only. Relevant prior edits inside those members cannot be treated as independently approved merely because this handoff focuses on the latest refinement. The existing audit and consumer map above supply context and limitations.

- `docs/design/engineering/validation.md` — `sha256:26ade9487d6db3f6c190b3f073af506431c6b47e1d7aab0f5b2c72cf897286f1`.
- `docs/design/system.md` — `sha256:45994453b7977569cfcf166f8543b492186b0932a1e8798c9c238c5e7e8623bc`.

A separate reviewer must assess the package without editing it. The author remains responsible for corrections; the reviewer determines their disposition. Concrete execution-plan authoring follows that review.

## Validation organization implementation — M1

Independent Design and Delivery Reviews approve bounded advisory reliance for this user-authorized refactor; their exact subjects and limits are recorded in `reviews/validation-refactor-design-review.md` and `reviews/validation-refactor-delivery-review.md`. The scoped plan is `docs/plans/2026-09-14-validation-test-organization.md`; no governed change was invented. Plan prose and selected validation passed after registering its path with Git intent-to-add (no file contents staged).

Before migration, the existing normal Python loader discovered 926 cases across 14 entrypoints; four imported release modules are included. This is discovery evidence, not execution success. Recovery sources, index and case identities were captured under `/tmp/validation-migration-baseline/` before edits. The existing audit remains historical to its source paths.

M1 moved ten Validation entrypoints to `tests/engineering/validation/` and six unchanged boundary fixtures to its `fixtures/boundary-first/`. Explicit root/module lookup, the metadata-suite caller, catalog commands and their bound hashes, exact old/new path classification, the executor workflow command and CLI's current metadata-test link were reconciled. No assertion or case was removed. Shared documentation-prose fixtures remain consumed by documentation and selector groups; shared record fixtures also remain at their existing capability roots. Production validators and executor remain in scripts.

The new routing regression failed before migration and passed afterward. Old and new suite paths select the same check IDs and current commands; unknown test paths remain unclassified. All baseline M1 case identities survived normal-loader discovery: 368 cases including one new selector regression, versus 367 before. All six moved fixture bytes were preserved. Direct runs passed eight suites; the selector's initial run exposed seven old metadata-path subcase failures, and all five affected methods passed after their caller correction. The executor's initial run passed 51 of 52 cases with one five-second child timeout during concurrent direct/selected runs; that case passed unchanged in a focused rerun and in the subsequent complete selected execution. No timeout or assertion was weakened.

The first selected attempt correctly rejected stale command/adapter basis hashes. Their explicit literals were rebound to the relocated commands; no runtime self-approval or hash-check bypass was added.

Post-correction `bash scripts/ci.sh --mode explicit --path scripts/validation_selection.py --path tests/engineering/validation/test-guide-system-validator.py --path tests/engineering/validation/test-boundary-first-validation.py --path .github/workflows/ci.yml` passed 306 check/case rows across 6 families. Actual local selector discovery returned status ok with no unclassified or blocking paths and included moved additions and old deletions. The emitted-style selector `-k` rerun passed; all 61 boundary tests passed on relocated fixtures, including invalid-input/proof fixtures.

Ephemeral detailed logs: `/tmp/validation-m1-ci.log`, `/tmp/validation-m1-discovery.json`, `/tmp/validation-m1-local-selection.json`, `/tmp/validation-m1-metadata-callers-green.log`, `/tmp/validation-m1-report-ownership-rerun.log`, `/tmp/validation-m1-negative-fixtures.log`, and `/tmp/validation-m1-rerun.log`. Actual results support M1 review, not final whole-change Verify, hosted CI or publication.


## Validation organization implementation — M2

M1 received independent scoped approval in `reviews/validation-refactor-m1-code-review.md`. M2 moved the remaining four entrypoints, four imported Release modules and Skill phrase helper to the exact destinations in the plan. Catalog commands, bound command hashes, production release-integrity commands, workflow callers and test fixture copies now use those locations. The publication workflow also accepts the actual old test path when its selected historical release tag predates the migration; it fails when neither path exists and propagates test failure. No callable alias was added. Contributor guidance, Skill references and the explicitly partial project map describe the observed layout.

Normal-loader discovery retains every one of the 926 baseline cases across 14 entrypoints and now discovers 929. The three additions protect old/new Validation routing, old/new Skill/Packaging/Release routing and historical-tag publication validation. The last case executes the actual parsed workflow shell for current, historical, both-present, missing and failing layouts. No existing case or assertion was removed. Package-native test sources remain unchanged in `packages/rigorloop/test/`.

The six boundary fixtures moved byte-for-byte with Validation. Shared `tests/fixtures/skills`, `adapters`, `documentation-prose` and `rigorloop-records-v3` remain available to their suites and selection/qualification fixtures. `tests/fixtures/release-transaction/current-version.json` remains a production release-preparation input/output contract, not disposable test-only data; its SHA-256 remains `94aec6847913d5566bf1ec5ad4b7a63b7a922f0d0121570e7a1d431506cf1bea`. The Skill phrase helper remains test-only and unchanged. Production implementation remains in `scripts/`.

Independent M2 review finding M2-CR-01 identified three stale commands in `scripts/validate-release.py`; they were corrected and the existing actual candidate case now asserts all three relocated commands in its successful receipt. Its owned source fixture copies the complete tests tree and current workflows so the candidate validates coherent current source. M2-CR-02 records historical-tag compatibility: a new regression failed against the unconditional new path, then passed with the selected-tag fallback. The reviewer owns their recorded dispositions in `reviews/validation-refactor-m2-code-review.md`.

### Combined current execution evidence

`bash scripts/ci.sh --mode local --broad-smoke --jobs 4` completed with 1,358 passed check/case rows and one failed candidate-integration row. Every selected row executed, including the broad archive build and validation; none was unstarted or suppressed. The command exited 1 and its boundary verdict remains unsuccessful. It is not reported as a passing full run.

That execution overlapped later additions to the package workflow regression and the candidate fixture's workflow copies. Its unaffected individual results remain applicable: production scripts, other suite sources, fixtures and environment were unchanged after selection. The changed package suite and failed candidate result were replaced by fresh executions after those edits stopped:

- `python tests/engineering/packaging/test-npm-package-publication.py`: all 8 cases passed in 27.868 seconds, including the new historical-tag case and its five outcome partitions.
- `python tests/engineering/release/test-release-transaction.py ReleaseCandidateIntegrationTests.test_actual_candidate_build_and_packed_metadata_chain`: passed in 73.292 seconds, including real source preparation, release-integrity callers, candidate archive, packed metadata and installed consumer checks. No publication occurred.

The earlier candidate failure is restricted evidence, not an unexplained pass. The stable corrected candidate proves the final copied source/workflow configuration; its real release-integrity execution also exercises the relocated Skill and Packaging suites. The full run's other 1,358 successful rows plus these fresh corrected checks cover the selected scope. Discovery counts are inventory, not execution totals. No hosted CI success is claimed.

Other correction proof retained: routing failed before path reconciliation and passed afterward; publication workflow changes originally failed closed as unclassified until added to its exact existing package category; unknown paths remain rejected. Explicit command-hash binding remains enforced. One local stale Python bytecode cache was removed after same-size rapid edits; no source or validation check was bypassed. The normal-loader/executor regressions passed in selected execution, including identical fixture population under ordinary and isolated bounded execution, single-case reruns and descendant cleanup. M1 direct proof and bounded selected proof retain the same fixture scope; no worker-limit or timeout assertion was weakened.

Detailed ephemeral logs are `/tmp/validation-m2-ci.log`, `/tmp/validation-final-package.log`, `/tmp/validation-final-candidate.log`, `/tmp/validation-final-discovery.json`, `/tmp/validation-m2-release-caller-red.log`, `/tmp/validation-m2-release-caller-green.log`, `/tmp/validation-m2-tag-layout-red.log` and `/tmp/validation-m2-tag-layout-green.log`. Durable results and limits above remain understandable without those logs. Independent M2 disposition, fresh whole-migration review and separate scoped Verify determine final reliance.


## Scoped verification of the completed validation migration

- Skill: verify; status: passed for this scope.
- Target: the complete M1/M2 migration under `docs/plans/2026-09-14-validation-test-organization.md` on `refactor/validation-organization`, at the 33 engineering identities in `reviews/validation-refactor-final-code-review.md`.
- Outcome: scoped-verification; execution mode: isolated; profile: VP0-scoped.
- Artifacts changed by Verify: this evidence section only.
- Open blockers: none within the migration.
- Next stage: return the completed scoped work to the user; no external handoff or lifecycle transition is selected.
- Readiness: scoped migration verified; whole-branch and hosted readiness are not assessed.

The separate final whole-change reviewer approved the complete migration after both milestone reviews and explicit closure of M2-CR-01/02. Verify checked that the exact reviewed engineering identities, System/Validation authority and reviewed plan still match current files. No engineering edits occurred after the final review. Workflow context reports successful discovery with no requested change identity; this result does not settle another initiative or invent governed state.

The evidence chain is complete for the authorized maintenance need. TEST-SR-17 and System placement are realized by all 14 relocated entrypoints, their four Release support modules, the Skill helper, six fixtures and reconciled consumers. TG-1/TG-2 prove runnable groups and actual production callers; TG-3 proves complete normal-loader retention, changed-path selection, negative detection, reruns and isolation. TEST-SR-15/16 remain proportionate: the three added regressions observe concrete migration/compatibility failures, while independent review assesses semantic claims. No universal skill-compliance assertion is made.

Verify independently compared case identities: all 926 original cases are retained among 929 current cases. All six moved fixtures and the Skill phrase helper byte-match the pre-migration baseline. Another 146 retained package-test, shared-fixture and executor files remain byte-identical. The release-owned current-version fixture remains unchanged. The actual package, archive and candidate observations above establish the affected generated-output and installed-consumer checks; static presence alone was not used as evidence.

The combined current execution evidence is adequate for the allocated scope: the full local/broad run's 1,358 unaffected passing rows, final package 8/8 pass and corrected real candidate pass. The failed aggregate invocation remains unsuccessful and is explicitly restricted; all selected boundary work actually executed. Both the independent reviewer and Verify assess the later package/workflow and candidate changes as covered by fresh exact runs, with unchanged observations retained only for unaffected surfaces. No further identical broad rerun is required solely to produce a different aggregate label.

Final `python scripts/select-validation.py --mode local` returned ok, with no unclassified paths or blocking results. Documentation prose enforcement passed for the refinement evidence, contributor guidance, Skill and project map; `git diff --check` passed. Current relative-link inspection found no new migration link defect. It found the pre-existing project-map link to absent `docs/explorations/`, confirmed unchanged in the pre-migration snapshot; that lies outside the explicitly partial test-layout refresh and limits whole-map claims.

The task's scope is complete: ownership-based test organization, compatibility-preserving callers, proportionate test design, navigation, independent review and scoped verification. Prior uncommitted work remains preserved; no content is staged, committed or published. Hosted CI and external release execution were not observed. The plan's local migration scope is satisfied without claiming whole-branch readiness, formal lifecycle completion or review of unrelated prior edits.

## Repository tooling organization refinement

The user authorized a follow-on organization of operational scripts after the completed test relocation. This scope preserves that working-tree baseline and existing command paths; it does not retarget the previous migration's exact-subject review or Verify. System, Engineering ENG-SR-16, Validation, Packaging and Release define the revised placement and preservation obligations. Independent `reviews/tooling-organization-design-review.md` resolved TDR-01's conflicting resource/implementation paths and approved scoped planning. `reviews/tooling-organization-delivery-review.md` approved `docs/plans/2026-09-14-tooling-organization.md`. No governed change identity, commit or external publication was selected.

Before implementation, source/index backups were captured under `/tmp/tooling-organization-baseline/`. Actual normal-loader discovery recorded 929 cases across 14 entrypoints. The concrete plan maps 18 Python/Node implementation modules and three authored resource files. The dual-use `scripts/release_evidence.py` retains its supported command as a thin wrapper; the implementation moves to Release. Four explicit package initializers establish capability-owned imports. Existing command implementations remain intact except their explicit imports; no wildcard re-exports or new universal command are introduced.

New regression proof initially failed for old/new tooling-path selection and nested source identity. Implementation reconciles imports and mock targets, worker bootstrap paths, Node helper URLs, resource/root resolution, copied selector workspaces and release source copies. A missing resource parent in the boundary fixture was exposed and corrected; its negative assertions remain. The selector's existing unsupported-script behavior classifies and blocks an unknown path, so the new Design wording and test preserve that behavior rather than incorrectly requiring it to appear in the unclassified array. This clarification requires the independent assessor's applicability judgment.

Release's former identity hashed only top-level Python/shell files. The revised identity uses relative paths for maintained Python/shell/Node files and authored resources recursively, excludes bytecode, and preserves before/after loaded-versus-source checks. Two focused cases pass: nested code/resources alter identity while bytecode does not; an actual temporary Git source/clone with changed nested release code or template is rejected before candidate build. These are local fixtures, without publication.

Current targeted evidence: routing regression passed; all 52 executor cases passed in 61.229 seconds, including identical ordinary/isolated case population and bounded concurrency; all 28 boundary-reference cases passed after fixture correction; three synthetic copied-workspace/selection cases passed. The three moved authored resource files byte-match the captured baseline. Contributor and partial project-map tooling references now name current paths. Full selected integration and independent final assessments remain pending at this entry.

### Tooling implementation corrections and proof applicability

The independent M1 reviewer recorded three material findings before correction. TCR-01 identified three serialized catalog IDs accidentally qualified by the import rewrite; their original values were restored, including test expectations. Isolated import of the captured baseline catalog and comparison with the current catalog now proves all 41 check IDs and command templates identical. TCR-02 identified a symlinked module directory omitted by recursive traversal; source identity now rejects root and nested symlinks before filtering maintained files, preserves missing-root failure, and continues to exclude runtime bytecode. TCR-03 identified initializer coverage narrower than actual descendants; four finite initializer paths now have explicit descendant check unions, separate from genuine relocation predecessors. Unknown initializer paths remain blocked. Each correction has failing-before and passing-after proof; the independent reporter owns disposition.

The final normal loader discovers 934 cases, retaining every one of the 929 baseline IDs. Five additions protect tooling move selection, initializer consumer coverage, recursive maintained-source identity, rejection of symlinked source directories and actual candidate rejection of changed nested source. No existing test assertion was weakened or case removed. Package-native tests remain unchanged. The full 96-case adapter suite passed in 253.787 seconds. Absolute invocation of `scripts/validate-skills.py` from `/tmp` validated all 19 canonical skills, confirming resource-root resolution. A real candidate integration passed in 81.903 seconds before the final catalog/identity corrections; that success is restricted to its earlier subjects and is not the final candidate proof.

`bash scripts/ci.sh --mode local --broad-smoke --jobs 4` began after TCR-01/02 correction and before TCR-03's initializer patch. Its selection used the earlier initializer mapping, but other changed descendants selected the required union. Its planned population omitted the subsequently added initializer test. Current focused initializer/move tests passed; a complete current selector rerun is required to replace affected earlier observations. The real candidate case was observed starting after final source freeze. Final source/test identities are captured in `/tmp/tooling-final-proof-subjects.json`; later evidence-only records do not alter them. The full run remains pending here and cannot yet supply a passing aggregate claim.

Detailed proof logs: `/tmp/tooling-catalog-red.log`, `/tmp/tooling-catalog-green.log`, `/tmp/tooling-symlink-red.log`, `/tmp/tooling-identity-corrected.log`, `/tmp/tooling-initializers-red.log`, `/tmp/tooling-initializers-green.log`, `/tmp/tooling-final-discovery.json`, `/tmp/tooling-adapters.log` and `/tmp/tooling-independent-cwd.log`. These ephemeral details supplement the durable results and limitations above.

### Completed current tooling validation

The full `bash scripts/ci.sh --mode local --broad-smoke --jobs 4` invocation exited 0 with all 1,364 selected check/case rows passed, no failed or unstarted rows, and a passed boundary scope. It exercised the actual prepared candidate after final source freeze, plus archive, npm, installed-consumer, release, Skill, record and executor proof. The earlier initializer selection basis remains explicitly scoped as described above; this passing invocation is not claimed to have discovered the later initializer case.

Fresh `python tests/engineering/validation/test-select-validation.py` then exited 0 with all 181 cases passed in 119.33 seconds, including complete current selector behavior and the new initializer regression. Fresh `python scripts/select-validation.py --mode local` returned ok, with no unclassified paths or blocking results. All final scripts/test identities remain unchanged since the recorded freeze. The full executed checks and fresh complete selector evidence together cover the final required scope. All 41 catalog IDs and command templates still match the pre-tooling baseline.

The five affected Designs pass structural/reference validation. Current plan, contributor and map prose checks pass; `git diff --check` passes. No hosted CI or public release is claimed. Detailed complete logs are `/tmp/tooling-full-ci.log`, `/tmp/tooling-final-selector.log` and `/tmp/tooling-final-local-selection.json`; durable counts, results and applicability are recorded here. Final independent whole-change review and separate scoped Verify remain the next assessment duties.


## Scoped verification of tooling organization

- Skill: verify; status: passed for the complete tooling scope.
- Outcome: scoped-verification; execution mode: isolated; profile: VP0-scoped.
- Target: `docs/plans/2026-09-14-tooling-organization.md` on `refactor/validation-organization`, at all 63 exact subjects in `reviews/tooling-organization-final-code-review.md`.
- Artifacts changed by Verify: this evidence section only.
- Open blockers: none within this scope.
- Next stage: return the completed work to the user; no external handoff or governed transition selected.
- Readiness: tooling organization verified; whole-branch, hosted and publication readiness not assessed.

Verify independently checked every final reviewed subject identity against the current file, the unchanged reviewed plan and current Design package, the explicit clarification reassessments, M1 approval, fresh final whole-change Code Review and resolved TDR/TCR findings. The nonauthor reviewer approved the complete final migration after implementation and correction. This assessment does not replace or retarget the preceding test-migration review/Verify. No change identity or formal lifecycle state was selected or mutated.

ENG-SR-16 and System layout are realized by stable top-level commands, eighteen relocated internal modules, four package initializers and three byte-preserved authored resources. Validation's selection/worker responsibilities, Packaging's real artifact/resource responsibilities and Release's candidate/source-integrity responsibilities remain with their existing owners. The plan's M1, TG-1 and TG-2 obligations are complete: normal-loader case identity retention, actual direct and isolated execution, exact path selection, all forty-one catalog IDs/templates, resource-root resolution, copied repositories, negative identity detection, actual candidate construction and packed/installed consumer proof.

Current execution evidence is adequate: the full local/broad invocation passed all 1,364 selected rows and boundary scope; the fresh final selector suite passed all 181 cases. The latter replaces the full run's earlier initializer-selection basis and includes its later case. Full-run candidate execution occurred after final source freeze, and all final scripts/test bytes remain unchanged. Independent ordinary discovery retains all 929 baseline cases among 934 current cases; five meaningful additions address actual migration and review-discovered failures. Resource bytes and package-native tests remain unchanged. Existing assertions were retained; no skipped case, weakened timeout, cache hit or unknown path was treated as success.

Final structure/prose checks and `git diff --check` pass. Current local selection reports ok without unclassified paths or blockers. The partial project map names inspected tooling paths; its unrelated, pre-existing explorations link limitation remains outside this refresh. No hosted CI result or external publication was observed. Local generated archive/package and installed-consumer results support their tested boundaries, not universal skill execution compliance.

The completed result separates commands, implementation and resources while preserving supported interfaces and strengthening protection for relocated source inputs. Prior user work remains intact on the existing branch. No file content is staged, committed or pushed; intent-to-add only made new source paths visible to ordinary diff/preflight checks. This scoped Verify closes the authorized tooling refactor without claiming completion of unrelated initiatives or whole-branch readiness.

## Risk-driven testing Design refinement

The user requested Design refinement first. This scoped amendment changes only the Validation Design, adding TEST-SR-18 and refining VAL-DEC-09 while preserving existing requirement and model identities. It defines selection of the smallest sufficient observation boundary, overlapping contract/public/composition/product descriptions, independent expectations, isolated fixtures, material failure and recovery scenarios, and criteria for broader proof and test maintenance. Observation boundaries are independent of executor phases. No new model, directory, catalog vocabulary, mandatory test layers or per-case ledger is introduced.

Related behavior and acceptance owners remain unchanged: Skill and Assessment retain semantic judgment, CLI/Records/Installation retain public behavior, and Packaging and Release retain actual artifact and required public observations. The method references these obligations rather than duplicating or weakening them. No test or runtime source was edited for this amendment; no suite audit, Delivery plan or implementation was initiated.

Independent [Design Review](reviews/risk-driven-test-design-review.md) approved the final Validation subject with no material findings: `sha256:200d5859395b87e7999e152f44c7d77a1da7d21d85ba028349f4fdd5eb70ed14`. This approval is scoped to the Design amendment. Earlier implementation reviews retain their historical subjects and are not reapproved against the new criterion.

Validation completed successfully:

- `python scripts/validate-boundary-first.py --check --path docs/design/engineering/validation.md`: passed structural/reference checks.
- `python scripts/validate-documentation-prose.py --mode enforce --path docs/design/engineering/validation.md`: passed with zero errors and warnings.
- `bash scripts/ci.sh --mode explicit --path docs/design/engineering/validation.md`: exited 0; selected checks passed. The run began before the final phase/cache wording clarifications; fresh direct Design checks cover the final bytes, and those clarifications changed no execution surfaces. Detailed output is in `/tmp/risk-driven-design-ci.log`.

The review recorded selected execution as pending; the successful completion above is subsequent author evidence. No formal lifecycle state, final Verify, branch readiness or external publication is claimed. Work stops at the requested Design refinement.

## Risk-driven test redesign planning

The user authorized the proposed next step of delivery planning after supplying a concrete test-redesign proposal. Created [the portable delivery plan](../../plans/2026-09-15-risk-driven-test-redesign.md) and linked it from `docs/plan.md`; no governed change was selected or initialized. Read-only `workflow-context --format json` returned successful complete discovery with no requested change. The attempted `workflow-context --help` was rejected as invalid input; no state mutation occurred.

The plan preserves the reviewed Validation Design and allocates four implementation milestones: bounded Skill pilot, Packaging fixtures/product proof, Release fixture/import dependencies, and Selection/execution boundaries. Every slice owns immediate discovery and caller reconciliation. Explicit replacement rationale prevents duplicate aliases from preserving counts at the expense of meaningful execution. Production import/default changes receive compatibility proof and return to Design if they require new public behavior. The supplied archive counts and prototype results remain limited external analysis, not current executed-suite proof.

Author checks passed: `python scripts/validate-documentation-prose.py --mode enforce --path docs/plans/2026-09-15-risk-driven-test-redesign.md --path docs/plan.md` reported zero errors and warnings; `git diff --check` passed. Initial explicit selection classified both paths but blocked on the new untracked authoritative plan. Scoped `git add -N -- docs/plans/2026-09-15-risk-driven-test-redesign.md` made the plan visible without staging its contents. Subsequent `bash scripts/ci.sh --mode explicit --path docs/plans/2026-09-15-risk-driven-test-redesign.md --path docs/plan.md` exited 0: `current_records.validate` and `guide_system.validate` passed. Details are in `/tmp/risk-driven-plan-ci.log`; these are planning checks, not execution of the proposed redesigned tests.

The plan subject is `sha256:687849e708b45cdf80e42ca3f12d7dbf168a64fd47a695bdffdb8dddc7a938e5`. Independent [Delivery Review](reviews/risk-driven-test-delivery-review.md) owns its judgment and exact applicability. No tests, runtime sources, Designs or previous plan bodies were edited in this planning step. No implementation completion, whole-change Code Review, final Verify, branch readiness, commit or publication is claimed.

## Risk-driven test redesign M1 implementation

The user authorized implementation after Delivery Review. This isolated slice implements M1 of [the reviewed plan](../../plans/2026-09-15-risk-driven-test-redesign.md), without governed milestone-state writes. The unchanged Design and plan retain their exact reviewed subjects; earlier implementation reviews do not approve these new test/selector changes.

The supported `test-skill-validator.py` aggregate imports three responsibility modules: recording-profile component cases, real command checks via a mixin, and canonical guidance checks. The normal loader discovers 289 unique Skill cases compared with 280 before M1. All 279 other selectors survive. The old `ExplicitRecordingGuidanceTests.test_targeted_pilot_reference_selection_and_failures` is replaced by `test_targeted_pilot_canonical_references_are_valid` plus nine `RecordingReferenceContractTests` cases. Each fault case runs both pilot targets with fresh minimal valid input and an exact diagnostic. The case count is discovery evidence, not a coverage or performance metric.

| Previous observation | Retained or strengthened proof |
| --- | --- |
| Both canonical pilot references pass without an inline heading | Canonical guidance case reads actual source and selected resources. |
| Missing body boundary and combined trigger mutation | Separate missing-boundary, missing-classification-trigger and missing-load-trigger cases. |
| Missing primary token | Minimal selected profile loses only `expected_revision`, with explicit mutation and diagnostic checks. |
| Missing resource despite unrelated resource and inline content | One independently runnable missing-resource scenario retains both non-rescuing distractions. |
| Escaped symlink, undecodable content and wrong selection | Separate fresh cases assert containment, decoding and selected-map diagnostics. |
| Selected resource must contain its profile section | New missing-profile-heading counterexample distinguishes readable irrelevant content from a usable profile. |
| Source-string approximation of target-runtime independence | Real entrypoint runs with empty PATH and with Codex/Claude/OpenCode traps; the marker detects attempted PATH dispatch even when errors are swallowed. Absolute executable paths and arbitrary agent behavior are outside this observation. |

The exact-text semantic checklist check moved unchanged into the guidance mixin. Assessment confirms semantic review ownership, but the inspected Design does not establish equivalent replacement protection for every existing phrase guard. Its contribution is therefore retained for investigation under TEST-SR-09, explicitly labeled a drift guard rather than proof of instruction quality. No canonical prose, raw-byte parity check, skill resource, package behavior or validator implementation was altered.

Selector consumers classify all three imported modules with the existing Skill-test owner. A new independently specified selector case requires the existing `skills.regression` and `adapters.regression` consumers once. Before classification, all three module paths failed this regression as unclassified. An initial test expectation also incorrectly demanded `skills.validate`; inspection of the existing owning category established that these test-helper changes select the two regression consumers, and the expectation was corrected without expanding policy. The focused regression then passed. Unknown-path rejection remains unchanged.

Proof so far: the pre-edit four-case guidance group passed; the refactored 14-case focused component/guidance/runtime group passed; the complete direct Skill aggregate passed all 289 tests in 21.971 seconds. Normal-loader before/after discovery accounts for the single replaced selector and all additions. Nine controlled validator branch defects each failed the intended component assertion for both targets, and a temporary real-entrypoint seed that swallowed a Codex invocation failed the trap-marker assertion. Seeded changes existed only in isolated process memory or temporary files; no production mutation remained. The restored direct suite passed. Details: `/tmp/risk-driven-m1-before/skill-ids.json`, `/tmp/risk-driven-m1-skill-ids.json`, `/tmp/risk-driven-m1-mutations.py`, `/tmp/risk-driven-m1-mutations.json` and `/tmp/risk-driven-m1-skill.log`.

The plan's shorthand directory argument `--path tests/skill` is not accepted by the current selector. Execution uses the plan's explicit-path expansion rule and enumerates all six changed files instead; no new directory-selection behavior is introduced. Full selector execution and selected CI remain pending at this entry. No complete M1 validation or review result is claimed until those observations and independent assessment are recorded.

### Completed M1 validation

`python tests/engineering/validation/test-select-validation.py` passed all 182 cases in 181.41 seconds. The complete explicit CI invocation was:

```bash
bash scripts/ci.sh --mode explicit --path tests/skill/test-skill-validator.py --path tests/skill/skill_contract_tests.py --path tests/skill/skill_cli_tests.py --path tests/skill/skill_guidance_tests.py --path scripts/lib/validation/validation_selection.py --path tests/engineering/validation/test-select-validation.py --jobs 4
```

It exited 0 with all 537 selected result rows passed, including 289 individual Skill cases, selector regression, executor regression and the retained adapter regression scope. No failed or unstarted rows were reported. This establishes selected reachability and execution under the existing worker budget in addition to the direct aggregate results; the scope is not the full product/release suite. The log is `/tmp/risk-driven-m1-ci.log`. No speed improvement is claimed.

All six engineering subjects remained unchanged throughout the completed direct and selected validation. Their final identities are:

| Subject | SHA-256 |
| --- | --- |
| `tests/skill/test-skill-validator.py` | `69b9e56a03a7488416b92bd27d2c3a022366f40205875b080eb84d018cc4e710` |
| `tests/skill/skill_contract_tests.py` | `6c0096c7323720a31d030661ad5359bdae809cfbe05a974497110c408ba3a15f` |
| `tests/skill/skill_cli_tests.py` | `ed93b91247c55f04e2cf1243d7c786224e4290e791ea7ca42e67e76b663d6a18` |
| `tests/skill/skill_guidance_tests.py` | `6be23c8be3db051f302a4c6d543f0936aee90e4baa446739c39185441519ea7d` |
| `scripts/lib/validation/validation_selection.py` | `ecc28c80d20b9aeaec50004d29a918910045d5e80c02687ff70522cb2bee2c5e` |
| `tests/engineering/validation/test-select-validation.py` | `31fba7a21b0094136456159e3fc16f292474110c8c43e3f2bab267dc044c2d2a` |

Independent [M1 Code Review](reviews/risk-driven-test-m1-code-review.md) owns the final slice judgment. The implementation evidence does not substitute for that assessment or for the later final whole-change Code Review and distinct Verify. M2–M4 are not implemented by this slice.

## Risk-driven test redesign M2 implementation

M1's recorded independent approval permits the next slice under the user's implementation authorization. M2 changes only the Packaging test suite and the Skill guidance module. The three discovery/proposal/targeted-profile archive happy-path groups are replaced by the retained independent complete-resource-inventory test, which builds actual archives once, validates them and runs actual packed-CLI installation for explore, research, proposal and proposal-review on both supported targets. The original valid selection subsets exercise the same per-selected-skill resource loop; their combined selection retains those outcomes. Two Skill guidance cases retain the exact canonical resources, discovery policy byte parity, portable output/path assertions and targeted primary-interface text constraints. Full archive inventory/byte or body parity retains resource transport, including Design assets. No generated expectation calls the producer's inventory helper.

Boundary-first generated-tree/archive/installed digest proof remains separate because it observes a distinct projection. Actual candidate metadata, all negative installers and installation safety cases remain. Archive-layer drift now uses one small skill plus an explicitly mapped reference in each private fixture, verifies the archive is valid before mutation, and preserves both reference-layer diagnostics. Selection-preflight cases no longer build archives: the intended unknown/duplicate input must reject before absent archive access, with no created output. There is no shared writable candidate, cached validation or runtime source change.

Independent review recorded RT-M2-CR-01: removing the discovery archive case lost the complete emitted-entrypoint forbidden-path check, since independent Claude parity excludes transformed frontmatter. Corrected the existing inventory case to check full emitted explore/research entrypoint bytes; canonical resource constraints plus exact transported resource bytes cover the remainder. Fresh corrected inventory/install proof passed in 16.512 seconds. This was a preservation miss corrected before M2 reliance, not an obligation retirement.

Normal-loader discovery accounts for Packaging 96→93 (exactly the three replaced groups) and Skill 289→291 (the two transferred guidance cases), with all other IDs retained. No executable consumer of the retired three method selectors was found; the retained inventory selector already belongs to the existing adapter regression selection. Current helper selection was established in M1 and remains effective.

Executed results:

- `python tests/engineering/packaging/test-adapter-distribution.py`: all 93 cases passed in 184.413 seconds.
- `python tests/engineering/packaging/test-npm-package-publication.py`: all 8 cases passed in 28.086 seconds.
- `python tests/skill/test-skill-validator.py`: all 291 cases passed in 21.408 seconds.
- `bash scripts/ci.sh --mode explicit --path tests/engineering/packaging/test-adapter-distribution.py --path tests/skill/skill_guidance_tests.py --jobs 4`: all 305 selected rows passed; required boundary scope passed.
- `python tests/engineering/release/test-release-transaction.py ReleaseCandidateIntegrationTests.test_actual_candidate_build_and_packed_metadata_chain`: actual prepared-candidate proof passed in 80.647 seconds. The initial invocation named `ReleaseCandidateTests` incorrectly and failed during unittest lookup; no candidate test body ran in that attempt.
- `python tests/engineering/packaging/test-adapter-distribution.py AdapterDistributionTests.test_distribution_archives_have_independent_complete_resource_inventory`: corrected final inventory/install proof passed. The earlier full/selected runs began before the review correction; this fresh execution covers the only changed test method, while all other executed surfaces remain unchanged.

Logs are `/tmp/risk-driven-m2-{packaging,npm,skill,ci,candidate-corrected,inventory-corrected}.log`; discovery and the baseline-relative diff are under the matching M2 temporary paths. This records actual execution and applicability without claiming speed improvement or public release. Exact final subjects:

| Subject | SHA-256 |
| --- | --- |
| `tests/engineering/packaging/test-adapter-distribution.py` | `3b87d65ba9625e562d0b8b5497fe03c0cce656f4b05c4dd60a4789f09313c3e4` |
| `tests/skill/skill_guidance_tests.py` | `ac28ed085d26772f1aaedb7454e0ef66a79336f5ca09509af31ee778ba40b970` |

Independent [M2 Code Review](reviews/risk-driven-test-m2-code-review.md) owns correction disposition and final slice judgment. Prior M1 review reliance is restricted for the changed guidance module; M2 assesses its added content and executed complete Skill suite. Later milestones and final whole-change assessment remain separate.

### Additional M2 counterexample

A temporary producer mutation inserted `templates/shared` into only the transformed explore description. The real archive validation and packed installation still ran, and the restored emitted-entrypoint assertion detected the defect exactly. `/tmp/risk-driven-m2-frontmatter-seed.py` and its `.log` record the bounded observation. The patch existed only in that process; engineering subjects were unchanged. Independent M2 review approved the correction and this supplemental observation.

## Risk-driven test redesign M3 implementation

M2's recorded approval permits M3 under the existing implementation authorization. Plain `approval_fixture()` returns fresh candidate, binding and nested provider facts with the same original values. Approval, execution and coordination fixtures consume it directly. Review RT-M3-CR-01 identified a remaining hosted-service fixture calling `ReleaseCoordinationTests.setUp()`; that correction now uses the same plain builder with the original protected-main branch, event updates and empty approval list. No `.setUp()` calls remain anywhere in the release test family. Other candidate and real-Git fixture setup remains owned by its tests; extracting unused generic builders would not improve an actual shared boundary.

Packaging no longer reads package metadata at module import. `default_adapter_version()` reads it when the build parser or missing-manifest support check actually needs a default. Both existing consumers were updated; explicit version selection, default version and help retain their command meaning, and malformed/missing metadata still fails at default resolution. No publication policy, approval comparison, retry, candidate sealing or persistence behavior changes. Current source search found no remaining Packaging constant consumers. The separate selector default is unrelated and unchanged.

New tests establish actual approval import and evaluation in an isolated source copy with missing or malformed package metadata; both conditions failed before the production change and pass afterward without fabricating valid metadata. Default-operation and parser tests independently specify missing/malformed rejection, `v9.8.7` from fixture metadata, explicit `v3.2.1` and help content. An initial help assertion treated line wrapping as meaningful; whitespace normalization corrected that incidental expectation without altering command output. The new release helper is explicitly classified by the selector, with a failing-before/passing-after regression for the existing release aggregate consumer.

Normal-loader discovery retains every prior ID: Release 179→180, Packaging 93→95, Selection 182→183. Focused approval six cases, default/parser two cases and selector helper case passed. Full direct Release passed 180 tests in 157.302 seconds; npm package proof passed eight tests in 26.796 seconds. Fresh `python tests/engineering/release/test-release-transaction.py ReleaseCoordinationTests ReleaseCandidateIntegrationTests` passed all 11 cases in 112.903 seconds after RT-M3-CR-01, including actual hosted prepared-candidate composition. This replaces affected pre-correction observations; other executed source/test surfaces remain unchanged. Full Packaging and selected execution remain pending at this entry.

Detailed M3 baseline, diff, discovery, red/green and execution logs use `/tmp/risk-driven-m3-*`. No runtime savings, release execution or whole-change completion is claimed. Independent M3 review owns finding disposition and exact-slice judgment.

### Completed M3 validation

Full `python tests/engineering/packaging/test-adapter-distribution.py` passed 95 cases in 315.031 seconds. The explicit CI invocation enumerated all eight changed file paths recorded below with `--jobs 4`; it exited 0 with all 429 selected rows passed and required boundary scope passed. No failed or unstarted rows appeared. The eight final engineering subjects remain unchanged after the hosted-fixture correction. The fresh 11-case coordination/candidate run covers that correction; earlier selected/direct results remain applicable only to their unchanged scopes. Full Packaging and npm cover the changed package-default consumers. No extra broad or public release claim is made.

| Final M3 subject | SHA-256 |
| --- | --- |
| `tests/engineering/release/release_execution_tests.py` | `dfb7b6829f9cec8676aedcc37e61871afc2bfccb0cd2b8ce89be916efcc6fb4b` |
| `tests/engineering/release/release_coordination_tests.py` | `7ab982ac924eded08a9d414bcacf95b4c0c287a13048ae634b6d58776a354cd3` |
| `tests/engineering/release/release_fixture_helpers.py` | `59c7b4eb22a6a2cc92c357f8e4bcc37cbd7c08309aab491e12e279e7d3e9ad18` |
| `scripts/lib/packaging/adapter_distribution.py` | `6f9d86e382d22d46453d0b7afa7128b043b1622f9be88cc8e82a4b1b43b19b0d` |
| `scripts/build-adapters.py` | `016bba889f4aecad94db38cf89db1bfcc2e22690d9b5ba6ba2b0257a5d8e1d20` |
| `tests/engineering/packaging/test-adapter-distribution.py` | `0d7c97ffe7d8e0728b935e3e18fdfb49dff391660886290442a76681d99b79fb` |
| `scripts/lib/validation/validation_selection.py` | `8d89ea493dd64a7c740b06216e419df98f8cc70bd83bd6d013f8db42a883fa20` |
| `tests/engineering/validation/test-select-validation.py` | `0dd913aa2b149671813f27a0d20c19e640e1327841de7bc5cc5eb3d4677bf7ca` |

Independent [M3 Code Review](reviews/risk-driven-test-m3-code-review.md) owns the final finding disposition and implementation assessment.

## Risk-driven test redesign M4 implementation

The selector aggregate retains its public TestCase/method selectors and existing runner. Non-TestCase mixins separate controlled contract cases, temporary-Git cases and actual CLI/CI cases; the shared helper owns fixture setup and the independently specified, unchanged EXPECTED_CATALOG. Four new module paths explicitly select the existing selector and executor checks. The new consumer regression failed before mapping and passed after it. Only the second expression in each of four identical adjacent assertion pairs was removed; their tests and protected assertion remain. No registry, framework or executor was added.

Synthetic case overlap now uses invocation-private marker rendezvous when two workers are allocated, with normal/sequential execution clearing the gate. Five active-counter callers similarly wait for recorded allocated overlap under the existing lock. Existing receipt identity, interval, worker cap and output observations remain. Deadlines bound broken handshakes; they are not speed targets. Distinct failure, timeout, barrier and cleanup timing fixtures remain intact.

Normal-loader discovery changes Selection 183→184 solely for the new module-consumer case, and Executor 52→52, with no original IDs lost. `python tests/engineering/validation/test-select-validation.py` passed 184 cases in 123.12 seconds. `python tests/engineering/validation/test-validation-execution.py` passed 52 cases in 66.205 seconds. Focused output/catalog checks passed 11 cases; two executor rendezvous checks and five counter-rendezvous cases passed. Sequential receipts show no overlap and concurrent receipts show overlap for the same two case IDs. Detailed source/discovery/direct proof is in the session's `/tmp/risk-driven-m4-*` evidence.

### Frozen whole-redesign subjects

The 17 paths below are the combined M1–M4 implementation scope. Their baseline is the earliest owning milestone's pre-edit snapshot, preserving the already-dirty branch. `/tmp/risk-driven-final.diff` contains only that combined redesign, and `/tmp/risk-driven-final-subjects.json` contains these exact current identities. Milestone observations remain applicable to unchanged surfaces; final selected execution assesses their combined current consumers. Evidence/report-only updates do not change these engineering subjects.

| Final engineering subject | SHA-256 |
| --- | --- |
| `scripts/build-adapters.py` | `016bba889f4aecad94db38cf89db1bfcc2e22690d9b5ba6ba2b0257a5d8e1d20` |
| `scripts/lib/packaging/adapter_distribution.py` | `6f9d86e382d22d46453d0b7afa7128b043b1622f9be88cc8e82a4b1b43b19b0d` |
| `scripts/lib/validation/validation_selection.py` | `e0239dc403a6a9f0ea0a2905c8268ebf0d015bd5767cbb7961ed35e5bd883c21` |
| `tests/engineering/packaging/test-adapter-distribution.py` | `0d7c97ffe7d8e0728b935e3e18fdfb49dff391660886290442a76681d99b79fb` |
| `tests/engineering/release/release_coordination_tests.py` | `7ab982ac924eded08a9d414bcacf95b4c0c287a13048ae634b6d58776a354cd3` |
| `tests/engineering/release/release_execution_tests.py` | `dfb7b6829f9cec8676aedcc37e61871afc2bfccb0cd2b8ce89be916efcc6fb4b` |
| `tests/engineering/release/release_fixture_helpers.py` | `59c7b4eb22a6a2cc92c357f8e4bcc37cbd7c08309aab491e12e279e7d3e9ad18` |
| `tests/engineering/validation/selection_cli_tests.py` | `585e25286619503f1e13c8d40516823f060bd16659e894fb98b3739c5ba6e56b` |
| `tests/engineering/validation/selection_contract_tests.py` | `b55487e8d28dd38374029f8b86c4a2017e81373ff84aa8d5f8e0f89bc606dcdd` |
| `tests/engineering/validation/selection_git_tests.py` | `8fb30b8fc9e969389dc5a8c37a3831dd925a54a8720836562aa6ed1114267b10` |
| `tests/engineering/validation/selection_test_helpers.py` | `4abe103954d342dd7436f6cde797d6cb882418fada02a1ac941f76199d3a16b7` |
| `tests/engineering/validation/test-select-validation.py` | `bdffcfb1966d0952d9536763e913886270233ec631cd9a2c4c249240e547f11e` |
| `tests/engineering/validation/test-validation-execution.py` | `b3bfb43b260bdb22ed6ec01a44259f0adf963b4da85a94f36fb425f8e0c6eb24` |
| `tests/skill/skill_cli_tests.py` | `ed93b91247c55f04e2cf1243d7c786224e4290e791ea7ca42e67e76b663d6a18` |
| `tests/skill/skill_contract_tests.py` | `6c0096c7323720a31d030661ad5359bdae809cfbe05a974497110c408ba3a15f` |
| `tests/skill/skill_guidance_tests.py` | `ac28ed085d26772f1aaedb7454e0ef66a79336f5ca09509af31ee778ba40b970` |
| `tests/skill/test-skill-validator.py` | `69b9e56a03a7488416b92bd27d2c3a022366f40205875b080eb84d018cc4e710` |

Actual local selection initially rejected the supplied untracked `tests.tar` as unknown input. The exact root attachment `/tests.tar` is now excluded only in local `.git/info/exclude`; no source-wide ignore rule changed. Archive bytes remain `sha256:9cac6531a5312b15f3833fb51673a69f5dd56190246f079f68d3bb152c339546`. Actual local selection subsequently returned no unclassified or blocking paths and no broad-smoke requirement. Its selected check set includes every check selected by explicitly enumerating these 17 source paths: adapters.drift, adapters.regression, adapters.validate, release_transaction.regression, selector.regression, skills.regression and validation_execution.regression. This establishes the combined scope before execution; selection alone is not a pass. The final local command also selects accumulated earlier branch changes, which are not attributed to this redesign.

### Completed final combined validation

`bash scripts/ci.sh --mode local --jobs 4` exited 0: all 1296 selected result rows passed, required boundary scope passed, and no failed or unstarted results appeared. The focused phase reported 1523.18 seconds (executor phase accounting, not a claimed runtime improvement). The local run includes actual package-native cases and all seven required check families from the explicit 17-path union. It therefore supplies the M4 combined-consumer execution and TG-FINAL-1 local integration evidence. Full output is in this session's `/tmp/risk-driven-final-ci.log`.

The approved Validation Design and delivery plan identities still match; all 17 frozen implementation identities match. `git diff --check` and documentation-prose enforcement of this evidence passed. This is local execution, not observed hosted CI, public publication or semantic skill-quality certification. Independent M4 and fresh final whole-change Code Review own their judgments; distinct Verify follows those completed assessments.

The explicit 17-path union and local selection have identical command strings for all seven shared check IDs, not merely matching names. The selected Packaging regression command runs 14 retained candidate/output cases; the unchanged full 95-case direct Packaging result above supplies its remaining rules and installer cases. Drift/validate requests share their matching successful executions under the existing executor. No claim of 95 Packaging cases in the final selected run is made.
