# Architecture-Review Semantic Preservation Review

- Rule ledger: `architecture-review-rule-disposition.yaml`
- Literal ledger: `architecture-review-literal-compatibility.yaml`
- Result: complete; no unexplained rule or literal disposition

## Rule ownership

All 20 semantic clusters have one final loaded owner. Universal purpose, evidence, surfaces, judgment, recording classification, compact result, stops, claims, and triggers remain inline. The C4, arc42, diagram, package, and ADR method clusters live only in `architecture-package-review.md`. Formal placement, identity, dispositions, prepared settlement, retry, recovery, and automation live only in `architecture-review-recording-and-settlement.md`. Measurement and package parity remain change-evidence or existing-validator responsibilities.

The references do not select review applicability, semantic status, materiality, workflow routing, or continuation. Loading is distinct from authority, and missing triggered resources stop without remembered reconstruction.

## Literal compatibility

All 20 literal rows retain their classified disposition. The four review surfaces, four statuses, severity vocabulary, four assembly names, and two exact resource verbs are present. `Finding ID:` and `Review status:` remain consumed labels. The shared `## Isolation and Recording` projection occurs once in `SKILL.md`, is absent from both references, and matches the normative template byte-for-byte.

Flat-file assertions that previously owned incidental architecture method or settlement prose now inspect the correct loaded package owner. No normative contract was weakened to preserve an obsolete physical location.

## Behavioral invariants

- One status governs the complete subject, while finding-scoped dispositions avoid unsupported target mutation and never create partial approval.
- Record-only surfaces bind exact subjects and bases and have no settlement targets.
- Formal evidence and the complete prepared manifest precede target transitions.
- Identical retry completes only pending matching writes and creates no duplicate review evidence.
- Advisory recording, manual isolation, workflow routing ownership, reviewer independence, and claim limits remain intact.
- No target-agent runtime, transcript grader, prose classifier, tokenizer dependency, or extra manual semantic-review gate was introduced.
