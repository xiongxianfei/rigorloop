# M4 Token and Package Proof

- Subject: versioned six-profile CLI result measurement, v0.4.x detailed baseline, operation documentation, and package validation
- Requirements: R29-R31, AC6-AC10
- Measurement: `python scripts/measure-cli-result-bytes.py --profiles packages/rigorloop/test/fixtures/observability/token-profiles-v1.json --baseline docs/reports/token-cost/cli/v0.4.x-detailed-baseline.json --check`
- Measurement tests: `python scripts/test-cli-result-measurement.py`
- Correction rounds: `code-review-m1-r1` finding `CLIOBS-M1-CR5` replaced fixture-asserted results with six real subprocess interactions. `code-review-m1-r2` finding `CLIOBS-M1-CR7` replaced the unsubstantiated comparison values with replayed detailed output from exact pre-feature revision `bcc7ef14ae45e8df737d8a97e72eff3a3823446b` and bound the baseline to that revision, package version, normalization rule, and detailed command mapping.
- Measurement result: all four gates passed from captured output; median reduction was 73.96%, per-profile reductions ranged from 64.19% to 83.90%, all required fields were observed, and the four continuation profiles required no follow-up. `default_changed` remains false.
- Measurement regression: `python scripts/test-cli-result-measurement.py` passed 4 tests, including rejection of asserted measurement fields and missing baseline provenance. The selector-owned token-cost regression `python scripts/test-token-cost-measurement.py` also passed 25 tests.
- Package documentation: configuration, platform-local storage, bounded rotation, exact lookup, privacy boundary, recovery controls, and explicit result projections are documented.
- Packed observability proof: `python scripts/test-npm-package-publication.py NpmPackagePublicationTests.test_packed_package_observability_surface_matches_documentation` passed 1 test. It packed and installed the npm artifact in a disposable root, executed the installed binary for default logging, exact lookup, console-off, file-off, concise-human, concise-JSON, detailed-JSON, and legacy JSON paths, and checked the installed README surface.
- Package regression: `npm test --prefix packages/rigorloop` passed 256 tests. `python scripts/test-select-validation.py` passed 154 tests. The exact C07 selection returned `status: ok`, no unclassified paths, no blocking result, and no broad-smoke trigger.
- Adjacent baseline debt: the broader selector-owned command `python scripts/test-npm-package-publication.py` ran 7 tests and failed 6 existing real-install cases because live canonical skills no longer reproduce the immutable bundled v0.4.1 archive sizes. A clean `origin/main` archive build independently reproduced the mismatch, while the exact M4-focused packed observability test passed. This is classified outside the authored M4 measurement and remains release/adapter follow-up; M4 does not rewrite historical v0.4.1 metadata.
- Proof-contract correction: approved plan review `plan-review-r3` and test-spec review `test-spec-review-r4` assign C08 to the focused packed-package test. `bash scripts/release-verify.sh v0.4.1` is no longer a feature-branch gate; immutable tag verification remains release-preparation work. Final verification separately owns C11 broad repository smoke.
- Result: the authored M4 measurement, package regression, selector proof, token-cost regression, and M4-focused installed-package observability proof passed. The isolated M4 implementation is ready for code review. The independently reproduced historical adapter-metadata drift remains explicit baseline debt and does not alter the measurement result. No default adoption, release readiness, or package publication is claimed.
