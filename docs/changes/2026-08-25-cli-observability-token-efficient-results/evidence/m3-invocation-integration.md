# M3 Invocation Integration Evidence

- Subject: public invocation correlation, default severity policy, exact log inspection, additive common formats, and package-fixture integration
- Requirements: R1-R5, R15-R20, R27-R28, R31-R32
- Focused tests: `node --test packages/rigorloop/test/cli-observability.test.js packages/rigorloop/test/cli-invocation-observability.test.js packages/rigorloop/test/result-renderer.test.js`
- Correction round: `code-review-m1-r1` findings `CLIOBS-M1-CR1` through `CLIOBS-M1-CR3` split non-mutating store validation from writer initialization, preserve semantic dispatch under unsafe paths and event failures, and defer projection until completion logging resolves.
- Direct matrix proof: public child-process coverage now exercises all five command families and validates correlated start/completion events, semantic status, exit code, and terminal severity. Controller tests cross success, blocked, and internal outcomes with info, warning, and error console thresholds.
- Failure-isolation proof: environment-off and console-off degraded cases preserve semantic output; lifecycle status is byte- and semantic-equivalent across recorded, disabled, unsafe-path, and lock-exhausted diagnostics; copied settlement claims in local logs cannot alter lifecycle status.
- Package tests: `npm test --prefix packages/rigorloop` passed 260 tests after the M3 review correction.
- Compatibility: routine success remains quiet on stderr, `--json` remains detailed, explicit concise output is opt-in, and `--no-file-log` changes only observability.
- Inspection: direct lookup of an absent store returns `RL_LOG_NOT_FOUND` without creating the directory. Exact lookup returns only the requested valid event and bounded codes for corrupt or unsupported unrelated records without returning raw corrupt content.
- Selector ownership: the exact C07 command now classifies the governed lifecycle wrapper and returns `status: ok` with no `manual-routing-required` result.
- Selector regression: C04 (`python scripts/test-select-validation.py`) passed all 154 tests after the two published skills received one compact validation-layering sentence each. Detailed command mechanics remain in contributor guidance rather than the skill bodies.
- M3 code-review correction: `CLIOBS-M3-R1-F1` through `CLIOBS-M3-R1-F4` now fail closed on non-canonical stored events, reject and suppress invalid invocation identities, classify terminal severity from semantic terminal class rather than exit code alone, and give the governed-lifecycle wrapper its own focused selector check. The focused Node command passed 69 tests, the wrapper matrix passed 5 tests, C04 passed 154 tests, and exact C07 selection returned `status: ok` with both `governed_lifecycle_cli_wrapper.test` and `rigorloop_cli.test` selected.
- Repository-wrapper status: `python scripts/validate-governed-lifecycle-cli.py` still exits 1 because the owning change record's `review.unresolved_items` does not match the open finding count recorded by the isolated M3 review. This correction does not edit lifecycle or review state; workflow reconciliation and a same-stage rereview remain required before milestone settlement.
- Lifecycle ownership recovery: the canonical governed-lifecycle CLI record was migrated to schema v2, the observability change's duplicate architecture registration was withdrawn without changing the architecture file, and six focused ownership/withdrawal regressions passed.
- Result: runtime correction, selector proof, and lifecycle ownership recovery passed; review-resolution and same-stage code rereview remain separate gates.
