# M2 Package Implementation

- Milestone: M2
- Status: implementation complete; review required
- Package: compact root, GitHub authoring reference, semantic risk map, and minimal skeleton
- Ownership: the risk map selects checks/boundaries; the GitHub reference serializes; the skeleton owns shape only.
- Privilege: mutation remains bound to one exact approved design and review.
- Claims: hosted observation is fixed to `not-performed-by-ci-maintenance`.
- Unchanged with rationale: adapter support metadata and repository workflows remain unchanged because existing build tooling discovers packaged resources and this milestone changes no hosted workflow.

Validation passed: focused simplification tests, canonical skill validation, the complete skill-validator suite, build tests, and `build-skills.py --check`.
