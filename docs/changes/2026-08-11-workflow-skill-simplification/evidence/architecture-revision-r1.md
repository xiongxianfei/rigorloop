# Architecture Revision R1

Finding resolved: `WFSIM-AR1`

The canonical `Architecture Decisions` section now records why workflow skill simplification requires no additional ADR.

The correction is intentionally narrow. It confirms that the change applies the existing mapped-resource package model and does not change the durable package model, `change.yaml` persistence, lifecycle ownership, runtime boundary, or deployment topology.

No architecture behavior, diagram, ADR, or specification requirement changed.

Validation target: `architecture-review-r2`
