# Plan Revision Evidence R1

Finding: CRSIM-PL1
Stage: plan
Date: 2026-08-10

M1 now names one exact Python-standard-library command over JSON-compatible YAML artifacts. It checks required ledger fields, applies the closed disposition vocabulary before destination consistency, verifies all seven required scenario identities and non-empty required/forbidden outcomes, and confirms the unknown-disposition fixture is outside the vocabulary.

The correction creates no validator file or permanent product invariant. It leaves the test spec free to map cases and evidence while preserving this concrete milestone command.

Ready for plan-review R2.
