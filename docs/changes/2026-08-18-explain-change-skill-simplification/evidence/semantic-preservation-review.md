# Explain-change semantic preservation

The final package was compared with the M1 rule and literal ledgers, the approved R1-R44 contract, and directly coupled workflow consumers.

## Rule disposition

All 26 stable rule IDs retain exactly one final owner and treatment:

- universal actual-diff, evidence, classification, action, resource, write, readability, stop, claim, and output safety remains in `SKILL.md`;
- governed eligibility, closeout, reviewed-subject identity, evidence-tail, staleness, and handback procedure resides in the governed reference;
- headings, order, metadata locations, tables, conditional-group location, and placeholders reside in the skeleton;
- workflow, verify, and PR remain the routing and readiness owners.

No rule was silently deleted, duplicated as an independent owner, or moved behind a resource that its common path cannot load.

## Literal disposition

All 15 literal IDs have one final treatment. Canonical explanation and legacy portable paths remain available; the root skill preserves the concise `review-resolution.md` summary and no-transcript-duplication contract while the governed reference owns closeout procedure; `Workflow handback` replaces misleading package-local readiness wording; EC0 through EC3 remain validator-consumed names. Existing readability, evidence-scan, closeout, and upstream-read-only consumers remain satisfied.

## Boundary and architecture

The approved input, state, authority, composition, temporal, recovery, compatibility, environment, and interaction rows have direct fixture or workflow proof. The implementation adds packaged text resources and narrows an existing Git allowance; it adds no persistent identity model, transaction record, schema, lifecycle state, routing owner, parser, generator, cross-stage write owner, or external integration. Architecture remains not required.

Acceptance uses deterministic contract, workflow, package, and distribution proof. It introduces no target-agent execution, transcript grading, manual semantic-review procedure, or prose grader.
