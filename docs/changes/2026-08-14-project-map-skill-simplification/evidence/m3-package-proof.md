# M3 Package and Preservation Proof

## Result

- Milestone: M3
- Status: implementation-complete
- Canonical resources: 3
- Semantic rules traced: 24
- Literal dependencies traced: 15
- Static scenarios retained: 35
- Target-agent runtimes executed: none

The final rule ledger points each behavior to its actual inline section, conditional-reference section, structural asset, or approved removal. The literal ledger preserves skill identity, resource-map syntax, statuses, headings, and asset path; migrates new results to `Operation` and `Map scope`; and retains the former four-mode form only as historical compatibility evidence. Deterministic scenarios and focused assertions cover classification, coordination, failure, transaction, evidence, commands, reliance, structure, and claims.

No separate manual semantic-review procedure is an acceptance dependency. The user assigned final human semantic judgment to ordinary PR review; this milestone proves the authored deterministic contract and package chain without claiming that later PR review has occurred.

## Package parity

`python scripts/test-adapter-distribution.py` passed. It generated and inspected supported adapter archives and install roots, including mapped skill resources and byte-parity failure paths.

The selected clean-install command built `v0.4.0` adapters in a fresh temporary directory and validated `project-map` for Codex, Claude Code, and opencode. Each generated package, archive, and temporary install contained the conditional reference and skeleton at the mapped paths with canonical resource identity. No published or tracked generated adapter tree was modified.

## Validation

- CMD1: passed with 24 rules, 15 literals, 35 scenarios, and unknown values rejected first.
- CMD2: canonical `project-map` skill validation passed.
- CMD3: 6 focused simplification tests passed.
- CMD4: 336 skill-validator tests passed with 16 skips.
- CMD5: 7 generated-skill build tests passed.
- CMD6: generated-skill check passed in temporary output.
- CMD7: adapter-distribution test suite passed.
- CMD8: selected `project-map` archive and clean-install validation passed for all supported targets.
- CMD9: boundary-first proof validation passed.
- CMD10 and CMD11: run at the M3 review handoff after lifecycle evidence is updated.

## Claim limitations

This evidence establishes deterministic contract preservation, profile reduction, and package parity for M3. It does not claim final holistic review, branch readiness, PR readiness, release readiness, publication, or completion of ordinary PR review.
