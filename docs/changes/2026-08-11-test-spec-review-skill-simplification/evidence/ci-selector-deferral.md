# CI Selector Deferral Evidence

Stage: ci-maintenance
Date: 2026-08-11
Trigger: final verify selector returned five `manual-routing-required` blockers

## Decision

The semantic ledger, literal ledger, and three static fixtures are one-change deterministic evidence with exact approved CMD1 and MP1 proof. They do not define recurring evidence classes.

The change records a complete repository-maintainer deferral for each exact path instead of adding a test-spec-review-simplification registry entry, selector branch, validator family, or broad fixture route. This uses the existing owner-deferral contract and preserves the approved non-goal against permanent simplicity infrastructure.

## Validation contract

- CMD1 remains mandatory for both ledgers and all three fixtures.
- MP1 remains the independent semantic-completeness check.
- Existing focused consumer assertions remain mandatory for migrated literal owners and package behavior.
- PR-mode selection must retain five visible `owner-deferred` registration-debt records and return no blocking result.
- PR-mode validation must execute every selected check successfully.

## Scope

No workflow file, check catalog, evidence-class registry, selector code, selector test, target runtime, permanent simplicity validator, or broad-smoke policy changed. The deferrals apply only to the five exact named paths and cannot match other evidence.

## Handoff

CI maintenance records routing ownership only. Validation execution and branch-ready judgment remain owned by `verify`. Because this support change follows final holistic review, a focused final rereview and refreshed rationale are required before verification resumes.
