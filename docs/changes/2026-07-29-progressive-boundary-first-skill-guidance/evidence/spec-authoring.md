# Spec Authoring Evidence

- Artifact ID: `spec`
- Artifact path: `specs/progressive-boundary-first-skill-guidance.md`
- Authoring stage: `spec`
- Completion status: `complete`
- Resulting review-request path: `docs/changes/2026-07-29-progressive-boundary-first-skill-guidance/reviews/spec-review-r1.md`

## Upstream settlement

The proposal entry is `accepted` under `proposal-review-r3`.
The review log has no open findings, and `review-resolution.md` is closed.

## Authoring result

The spec creates one focused amendment contract for the boundary-first proof
model, skill contract, workflow contract, and change-scoped validation
contract.
It preserves every non-conflicting requirement and supersedes only the
full-reference, explicit-trigger, progressive-consumption, scenario-selection,
and skill-path artifact-lifecycle-routing behavior selected by the accepted
proposal.

The contract defines:

- a prompt-independent four-question compact scan;
- active-only automatic formal adoption;
- one compact core plus feature-authoring and proof stage-family resources;
- exact stage-owned artifact-slice consumption and upstream gap routing;
- distinct-outcome and material-hazard scenario selection;
- purpose-built skill validation without skill-path lifecycle settlement;
- retained lifecycle validation for governed artifacts and mixed changed sets;
- deterministic canonical, generated, packed, and installed parity;
- atomic activation, historical compatibility, and coherent rollback; and
- structural-versus-semantic claim boundaries.

## Boundary-method decision

The repository capability remains `pending`, so this spec does not claim the
activation marker required for an active adopting feature.
It nevertheless records the complete `boundary-first-v1` applicability,
boundary, interaction, and example-ownership model for semantic review.
The semantic version remains unchanged because this refinement changes
resource composition and consumption rather than the closed vocabulary,
identifier grammar, feature record, or proof record.

## Validation target

The authoring target is independent `spec-review-r1`.
The review should challenge normative clarity, pending-versus-active behavior,
resource ownership, stage authority, scenario sufficiency, selector
composition, compatibility, rollback, privacy, and the semantic completeness
of the boundary record.
