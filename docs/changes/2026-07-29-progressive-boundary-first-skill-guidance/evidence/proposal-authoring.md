# Proposal Authoring Evidence

- Artifact ID: `proposal`
- Artifact path: `docs/proposals/2026-07-29-progressive-boundary-first-skill-guidance.md`
- Authoring stage: `proposal`
- Completion status: `complete`
- Resulting review-request path: `docs/changes/2026-07-29-progressive-boundary-first-skill-guidance/reviews/proposal-review-r3.md`

## Authoring result

The proposal records the selected direction from the preceding exploration:
automatic compact boundary awareness, conditional formal records, one authoritative model, stage-family progressive guidance, stable-ID downstream consumption, and hazard-driven scenario selection.

The proposal preserves the accepted portable boundary-first direction while treating resource composition, load conditions, prompt independence, and downstream context scope as the change under review.

The refined proposal also records validator ownership:
published skills use purpose-built skill, projection, adapter, boundary, and prose checks;
artifact-lifecycle validation remains scoped to governed artifacts and resolves current mutable state from the owning stage-owned change record.
The stage-owned validator correction is separated from the boundary-guidance implementation so each change remains independently reviewable.

The R1 revision accepts `PBS-PR1` and separates two delivery paths.
The embedded-status correction may proceed independently under the already approved stage-owned lifecycle requirements.
Skill-path selector removal and every new boundary-guidance behavior now follow amended feature contracts and `spec-review`, architecture assessment and review, plan review, and the later test-spec and test-spec-review before implementation.

The R2 revision accepts `PBS-PR2` and restores stage-owned artifact order.
Feature contracts settle through `spec-review`, architecture settles through `architecture-review`, and the execution plan settles through `plan-review`.
One matching test specification is then authored from those approved inputs and settles through `test-spec-review` before implementation.
`spec-review` no longer receives authority over test-spec content, and proof-map work is not duplicated before and after planning.

## Validation result

- Change metadata validation: passed.
- Change metadata regression suite: passed with 60 tests.
- Markdown readability validation: passed with 73 non-blocking audit warnings.
- Whitespace validation: passed.
- Artifact lifecycle validation: passed after the separate stage-owned validator correction, with one non-blocking generic lifecycle-language warning for the proposal.
- R1 finding disposition: `PBS-PR1` accepted and confirmed resolved by proposal-review R2.
- R2 finding disposition: `PBS-PR2` accepted and revised; proposal-review R3 is the validation target.
