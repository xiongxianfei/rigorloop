# M1 Preservation Inventory Evidence

Milestone: M1
Date: 2026-08-12
Profile: `IP2-planned-armed`

M1 inventories the complete current `skills/spec-review/` package before any canonical skill prose moves. The semantic ledger has 18 stable rule clusters, the literal inventory has 18 classified dependencies, and the scenario fixture has exactly the 17 approved scenario identities.

The inventory separates universal review, recording, status, isolation, stop, and claim behavior from governed settlement and automation procedure. It keeps the two checked boundary references under their existing ownership, keeps the two assets structural, and identifies repeated output enumeration as the duplicate to remove.

Bounded consumer inspection covered skill and review validators, generated-skill tests, adapter/package tests, formal-review recording contracts, boundary projection consumers, workflow automation consumers, and the result/finding assets. The shared `## Isolation and Recording` block remains a byte-identical inline parser/package contract.

Validation uses the exact CMD1 command from the approved plan and test specification. No target-agent runtime, network service, publication action, generated package mutation, or canonical skill edit is part of M1.

Unchanged required surfaces: `skills/spec-review/`, shared contracts, validators, generated packages, and adapter packages are unaffected in M1 because this milestone freezes evidence only.
