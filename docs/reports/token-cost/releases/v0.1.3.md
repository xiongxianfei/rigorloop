# Token-Friendliness Report v0.1.3

YAML metadata: `docs/reports/token-cost/releases/v0.1.3.yaml`

## Summary

Static canonical skill measurement still reports 56,615 estimated tokens across 23 skills, with the same warning-only high-cost skills as v0.1.2: `skills/workflow/SKILL.md` and `skills/code-review/SKILL.md`.

The dynamic suite was run in approved dry-run mode after live local Codex execution stalled before completing all prompts. The dry-run evidence validates the v0.1.3 benchmark source contract: generated public Codex adapter output from the release archive was installed into the fixture, and `.codex/skills/` was not used.

## Release Gate

Result: warning.

Warnings remain follow-up work for the separate token-cost optimization slice; they do not block the adapter untracking release evidence.
