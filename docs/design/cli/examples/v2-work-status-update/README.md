# V2 work status update

Historical, non-operational illustration. V2 storage and its validator are retired; preserve the example bytes and original meaning without executing or converting them.

The [request](request.json) and [saved response](response.json) illustrate current `rigorloop work set work-1 --root . --change example-change --input - --format json`, under [CLI-SR-03/12/16](../../cli.md#requirements).

The omitted v2 store already contains pending work-1 at the synthetic `d` revision. No extra engineering subject basis is claimed for this status-only actor decision. The request replaces status with in-progress; ownership, requirements and neighbors remain unchanged. The response's `e` revision represents the saved after-state, not a computed fixture hash. Storage success does not choose or authorize the activity.
