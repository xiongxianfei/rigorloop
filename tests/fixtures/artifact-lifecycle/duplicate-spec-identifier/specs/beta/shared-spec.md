# Shared Spec Beta

## Status
- approved

## Goal and context

Exercise duplicate top-level spec identifier detection across paths.

## Requirements

R1. The validator MUST reject duplicate spec identifiers.

## Acceptance criteria

- The fixture fails when both duplicate specs are validated together.

