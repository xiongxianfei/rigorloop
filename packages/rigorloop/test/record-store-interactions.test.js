import {test} from 'node:test';
import assert from 'node:assert/strict';
import {runInteraction,SCENARIOS} from './helpers/record-store-interactions.mjs';
for(const scenario of SCENARIOS)test(`TG-08 ${scenario} has equivalent complete interaction semantics`,()=>{const advanced=runInteraction(scenario,'advanced',{guidance:false}),targeted=runInteraction(scenario,'targeted',{guidance:false});assert.deepEqual(targeted.semantic,advanced.semantic);assert.deepEqual(targeted.basis,advanced.basis);assert.deepEqual(targeted.checkpoints,advanced.checkpoints);if(scenario==='applicability-reassessment'){assert.equal(targeted.checkpoints.length,2);assert.equal(targeted.checkpoints[0]['docs/changes/example/change.json'].applicability.find(a=>a.path.endsWith('design-review.json')).value,'stale');assert.equal(targeted.checkpoints[1]['docs/changes/example/change.json'].applicability.find(a=>a.path.endsWith('design-review.json')).value,'current');}assert.ok(targeted.calls.includes('context'));assert.ok(advanced.calls.includes('record-store.inspect'));if(scenario!=='final-explanation-read')assert.ok(targeted.calls.includes('subject.inspect'));});
